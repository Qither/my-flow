# Separate follow-ups

These are later independent proposals, not unchecked work required for this core delivery.

| Follow-up | Entry evidence to refresh | Acceptance boundary |
| --- | --- | --- |
| Optional jj plugin | Git adapter contracts after this change; local jj version; official Git/Windows/workspace docs | Writer changes cannot move verifier's tested snapshot; recover a failed attempt; preserve links/reports; test .gitattributes differences and Git coexistence; core works without plugin |
| Browser close audit | Current `my-flow-browser/driver/actions.mjs` and gateway drainBlocked call; current browser spec | Real close produces required blocked audit records, demonstrated in runtime test; reconcile contract before implementation |
| Browser spec housekeeping | Current browser-harness Purpose and archived deltas | Replace only remaining placeholder purpose/omitted requirements with evidence-grounded descriptions |
| Stagehand maintenance | Current fork/upstream and archived work status | Propose only still-relevant fixes with independent checks; no resurrection of already completed audit cleanup |

Do not initialize jj, edit sibling repositories or create these as active goals in this plan.
