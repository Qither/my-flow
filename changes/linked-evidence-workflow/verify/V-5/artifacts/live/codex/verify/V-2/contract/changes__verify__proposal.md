## Why

Exercise a fresh independent verification after adding a required live criterion to an initially automated-only fixture. Old automatic evidence must not grant the later live claim.

## What Changes

Exercise a fresh independent verification after adding a required live criterion to an initially automated-only fixture. Old automatic evidence must not grant the later live claim.

## Non-Goals

No changes to the candidate plugin, host configuration, other fixture changes, or external files.

## Decision Boundaries

The initial automated contract is verified by a real independent agent before the live requirement is added. Never fabricate an old PASS. After adding the live criterion, first observe INCOMPLETE with only old static evidence, then obtain an actual native-verifier read of native-probe.txt and a fresh PASS.
