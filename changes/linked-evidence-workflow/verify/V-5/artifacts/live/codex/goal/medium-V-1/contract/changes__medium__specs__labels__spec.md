## ADDED Requirements

### Requirement: Ready label exports

#### Scenario: Import both labels
- **WHEN** the left and right modules are imported
- **THEN** their named label exports equal `Left ready` and `Right ready` respectively.

