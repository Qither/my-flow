# V-5 amendment observations and bounded supplement

Native independent observer `/root/v3_live_observer` verified the following on both original
hosts: the supplied F-1 attempts were honestly marked fixture inputs; design review preceded
any third patch; staged equivalent-check A-1 preserved the old canonical digest while pending;
A/B were reopened and held; C alone passed its fixed check, was ticked and refreshed its lease;
the configured independent critic approved the candidate before apply; only AC-01's command
changed; A/B stayed unchecked and C checked. All protected source/test/script hashes matched.

The original fixtures had no old verification attempt or evidence link. Therefore those live
runs do not prove historical evidence retention/removal, despite proving the other behavior.
This is a preparation omission. Original runs remain intact; A/B must not be completed just
to make the fixture look finished. The observer approved a minimal separate history supplement
using the same old/candidate digests and transparently reused original review judgments.

Each original workspace now has `.my-flow/amend-history/`, a new nested Git root cloned from
its pre-amend full snapshot. The observer independently ran all three fixed amend checks once
per clone (all 1/1, 18/18 pinned hashes unchanged), then the parent recorded a real V-1 PASS.
The requests use writer `fixture-history-baseline-writer`; results use native independent
actor `/root/v3_live_observer`, full end digests and a copied verifier-report artifact.
No fake PASS or old result file was copied in as a new verification.

The prepared `run-amend-history.mjs` is restricted to an original native cwd/session and the
new nested root. Its supplement reuses the old role judgments with their actual actor/source
references, adds valid V-1 bookkeeping links, and uses real CLI propose/review/apply mechanics.
It checks immutable old V-1 files, removed A/B links, retained C link, old captured acceptance,
new current acceptance and read-only archivePreflight's contract-drift rejection.
It does not replay planning, authentication, goal checks or independent model reviews.

Low-level evidenceStatus checks durable head integrity and need not itself return stale.
The composite archivePreflight and explicit old/current target comparison establish that the
old proof remains historical and cannot authorize archiving the changed contract.

For the final verifier behavior fixture, each original `verify` change retains its real
automated-only V-1 PASS. Authorized fixture preparation added required live AC-02 and unchecked
T-02, with T-01 retaining its V-1 link. Preparation and old contracts are saved under
`observations/verify-live-preparation/`. The first native phase will perform automated AC-01
only and preserve missing live AC-02 as INCOMPLETE; the next phase will authorize the fresh
native read, followed by a new independent PASS and the task tick after actual verification.
Original scoped amendment leases were released using the CLI; their stage/tasks were not
falsely marked done.
