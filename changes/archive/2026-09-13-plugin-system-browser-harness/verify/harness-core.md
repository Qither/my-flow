## Verification Report

### Change: harness-core at 0f5976f8000227a0627ccfac25fbfca7bf0f7987, 2026-09-11T14:35Z to 14:46Z

### Verdict: PASS

Working tree at the start and end of this pass: only ` M changes/harness-core/tasks.md` (the 8.3 unblock note, read in full). Every command below was run in this pass in `H:\CommonProject\workflow\VibeCoding\my-flow-browser`. No earlier report's result was used as evidence; the three commits after 8d (004ad7c, 5cdd638, 0f5976f) were read and their code exercised by the full suite, a fresh network audit, and the window-visible measurement. The frame-enumeration completeness gap is excluded by the user's decision recorded in tasks.md 8.3 and design.md ("Closed by decision, 2026-09-11"); nothing else was excluded.

Provenance: produced by the read-only `my-flow:verifier` role in a fresh context (agent `verify-harness-core-9`) at the request of `/my-flow:mf-verify harness-core`, and saved verbatim by the caller (the verifier role cannot write files). Commit range covered: 279a665..0f5976f.

### Evidence (command -> result)

- `git rev-parse HEAD` -> `0f5976f8000227a0627ccfac25fbfca7bf0f7897`; `git status --short` -> ` M changes/harness-core/tasks.md` (before and after the pass)
- `npm test` (full suite, background, ~6 min) -> exit 0; `ℹ tests 173`, `ℹ pass 173`, `ℹ fail 0`, `ℹ cancelled 0`, `ℹ skipped 0`, `ℹ todo 0`, `ℹ duration_ms 359594.8`; 173 `✔` lines, 0 `✖`
- `node --test --test-concurrency=1 test/policy.test.mjs test/mask.test.mjs test/approval.test.mjs test/ratelimit.test.mjs test/audit.test.mjs test/server.test.mjs test/generate.test.mjs test/manifest.test.mjs` -> `tests 81`, `pass 81`, `fail 0`, `skipped 0`
- 8.3(a): `grep -c "no defect found" .my-flow/verify/harness-core-verify-8d.md` -> `1` (line 8: `Verdict: **INCOMPLETE - no defect found.**`)
- 8.3(b): `grep -c "e2e pending" .my-flow/verify/harness-core-e2e-claude.md` -> `0`; same for `-codex.md` -> `0`; `grep -c "Verdict:"` on both -> `0` and `0`
- `node H:/CommonProject/workflow/VibeCoding/my-flow/scripts/cli.mjs plugin list` -> `my-flow-browser 0.1.0 [enabled]`, `setup: ok`, `claude: installed`, `codex: installed`, `verbs: browser`, `mcp: my-flow-browser: node .../server/mcp.mjs`; exit 0
- Host evidence cross-check against the real audit log `%USERPROFILE%\.my-flow\browser\audit\2026-09-11.jsonl`: records with `"host":"claude-code"` -> `11` (file says eleven); `"host":"codex-mcp-client"` -> `9` (file says nine); approval token `f7255b3d...` -> `2` lines, token `1dcb2736...` -> `1` line, matching the excerpts
- `grep -c "^\[mcp_servers.my-flow-browser\]" ~/.codex/config.toml` -> `1`; `startup_timeout_sec = 30` at line 183
- Section 1: `my-flow-plugin.json` -> `my-flow-browser 0.1.0 my-flow-browser- ["skills","agents","hooks","cli","mcpServers"] npm ci node_modules`; `grep -c mcpServers .claude-plugin/plugin.json` -> `0`; `test ! -e .mcp.json` -> ok; marketplace `source` -> `["./"]`; `agents/browser-operator.md` frontmatter has `disallowedTools: Write, Edit, MultiEdit, NotebookEdit`; `hooks/hooks.json` SessionStart -> `node "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/browser-status.mjs"`; `MY_FLOW_HOME=<empty tmp> node <my-flow>/scripts/cli.mjs plugin add <repo> --dry-run` -> `[dry-run] would add my-flow-browser 0.1.0 (...)`, exit 0
- Section 1.3: `MY_FLOW_PLUGIN_ROOT=$PWD node scripts/cli.mjs browser doctor` -> `chromium: C:\Program Files\Google\Chrome\Application\chrome.exe`, `REMOTE_ALLOW_ORIGINS: chrome-extension://pljmheomijcfbgcbiccbbgehjckjnlij`, `node_modules: present`, `gateway: not configured`, exit 0; `MY_FLOW_BROWSER_CHROME=C:/nowhere/chrome.exe node scripts/cli.mjs doctor` -> `chromium: not found at C:/nowhere/chrome.exe`
- Section 2: piped `initialize`, `notifications/initialized`, `tools/list`, `tools/call browser_nope` into `node server/mcp.mjs` under a temp `MY_FLOW_HOME` -> exit 0; stdout exactly 3 complete JSON lines (ids 1, 2, 3); 12 tools, sorted names `browser_back, browser_click, browser_extract, browser_observe, browser_open, browser_read, browser_screenshot, browser_tabs, browser_type, browser_unsafe_cdp, browser_unsafe_cookies, browser_unsafe_evaluate`; `untrusted` present in the descriptions of observe/read/extract/screenshot; `browser_open` `required: ["url"]`, `additionalProperties: false`; id 3 -> `{"error":{"code":-32602,"message":"Tool browser_nope not found"}}`; stderr `ready (12 tools, no browser started)` then `shutdown (stdin end); spawned: 0`; `<tmp>/browser/state` -> 0 files
- Section 4 spike record: `grep -c "^spike [abc]" changes/harness-core/design.md` -> `3` (a, b, c each `1`); `grep -n "REMOTE_ALLOW_ORIGINS =" driver/chromium.mjs` -> line 30 `DEFAULT_REMOTE_ALLOW_ORIGINS = 'chrome-extension://pljmheomijcfbgcbiccbbgehjckjnlij'`, line 31 override wrapper; `driver/session.mjs` -> `REPLAY_MODE = 'act'`, `TARGET_SOURCE = 'cdp-dom'`; `ls scripts/spikes/` -> `allow-origins.mjs cdp-inspect.mjs generate-refusal.mjs snapshot-fixtures.mjs tree-dump.mjs window-visible.mjs`
- Section 6 committed report `.my-flow/verify/harness-core-network-20260911T130540Z.md`: `only loopback destinations:` count `1`, `Verdict:` count `0`, table header `| destination | port | via | count | first seen |` at line 12; all `node fetch`/`node net.connect`/`node websocket` rows are `127.0.0.1`; every non-loopback row is `via proxy` (refused). Its flag block equals, line for line, the output of `chromiumArgs({profile:'Agent-General',headless:true,audit:{proxyPort:60387}})` at HEAD
- Fresh re-run at HEAD: `MY_FLOW_HOME=<scratch> node scripts/cli.mjs browser audit --scenario fixture --out <scratch>/network-fresh.md` -> exit 0, `scenario completed: yes`, `only loopback destinations: no`, harness rows (`node fetch` 4, `node net.connect` 5, `node websocket` 4) all `127.0.0.1:65046`; 9 non-loopback destinations all `via proxy` (Google background hosts plus the scenario's own `example.com`/`example.invalid` probes), `Verdict:` count `0`; 0 `chrome.exe` left afterwards
- Timing of the committed report vs the driver: `git log -1 --format=%cI -- driver/chromium.mjs` -> `2026-09-11T21:48:27+08:00` (0f5976f); report `Started: 2026-09-11T13:05:40Z` (21:05:40 local), mtime 21:05:45; file mtime of `driver/chromium.mjs` is 21:22:30 (the agent-window-visible verifier's stash/pop rewrote the file; content is byte-identical to what was audited, and the fresh re-run above removes any doubt)
- `node H:/CommonProject/workflow/VibeCoding/my-flow/scripts/spec.mjs validate harness-core --root H:/CommonProject/workflow/VibeCoding/my-flow-browser` -> `ok   harness-core`, `1 passed, 0 failed`, exit 0
- Main spec scenario: `MY_FLOW_HOME=<scratch> node scripts/spikes/window-visible.mjs` -> `profile: Agent-General`, `Chrome_WidgetWin_1|True|1280|800|... - Google Chrome`, `main window visible: true`, exit 0; 0 `chrome.exe` on that home afterwards
- Do-Not-Touch: `git log --name-only --format= 279a665..HEAD | sort -u` -> 17 paths, all inside this repo (archive of agent-window-visible, `changes/harness-core/*`, `changes/request-interception/*`, `driver/chromium.mjs`, `driver/session.mjs`, `package.json`, `scripts/spikes/window-visible.mjs`, `specs/browser-harness/spec.md`, `test/helpers.mjs`, `test/regression.test.mjs`); whole-history `sort -u` -> 92 paths, none outside the repo; `changes/.templates/*` appears only in the initial scaffold commit 049b10f and never again; `git -C H:/CommonProject/workflow/VibeCoding/my-flow status --short` -> `?? changes/`, `?? specs/` only (untracked dirs dated 02:24, before this change's first commit at 03:38; no tracked file modified); `grep -rn "User Data" driver/ gateway/ server/ scripts/ llm/ audit/` -> only `driver/chromium.mjs:117-119` inside `dailyProfiles()`, the deny list
- Fake-completion grep over server/ gateway/ driver/ llm/ audit/ scripts/ test/ skills/ agents/ hooks/ -> two hits: `llm/generate.mjs:128` `'model gateway configured but not implemented until sub-change 4'` (a deliberate fail-closed refusal with `code: 'NO_GATEWAY'`, covered by `test/generate.test.mjs:131`), and that test's own assertion. `return null` in exported functions -> `gateway/policy.mjs urlHost` (unparseable or non-http URL) and `driver/observe.mjs parseTreeLine` (non-matching line); both are legitimate parse results, not stubs. No `TODO`, `FIXME`, `test.skip`, `.skip(`, `.only(`
- Package: `npm ls --depth=0` -> `@browserbasehq/stagehand@4.1.0`, `@modelcontextprotocol/server@2.0.0`, `zod@4.6.1`, exit 0, no missing/invalid; `package.json` dependencies are exact pins, no devDependencies; test script lists 15 files; `ls test/*.test.mjs` -> 15 files, every one present in the script
- Docs (7.4): `grep -c browser_unsafe_evaluate README.md` -> `1`; `grep -c "npm test" CLAUDE.md AGENTS.md` -> `1` and `1`
- 004ad7c reviewed: live frame map keyed by `targetId`, an attached-but-unnavigated iframe recorded as `UNKNOWN_FRAME` (gateway refuses), `waitForDebuggerOnStart: true` plus `Runtime.runIfWaitingForDebugger` per child session, main-frame commit rebuilds history from the live map; new test `a frame that has attached but not yet navigated makes the tab unknown, not clean` and `waitFor(rel)` in the fixture server replacing fixed sleeps. Both host e2e runs (10:32Z, 11:27Z) post-date this commit (09:26Z)

### Criteria (id | VERIFIED / PARTIAL / MISSING | evidence)

Tasks (tasks.md)
- 1.1 | VERIFIED | manifest fields, `mcpServers` count 0, no `.mcp.json`, dry-run add exit 0 with expected line
- 1.2 | VERIFIED | `npm ls` resolves the three pins; fixture-server and import checks covered by `test/manifest.test.mjs` (3 pass) and the full suite
- 1.3 | VERIFIED | doctor output, both `browser doctor` and `doctor` forms; `test/chromium.test.mjs` 9 tests in the passing suite
- 2.1 | VERIFIED | handshake pipe above; `test/server.test.mjs` 9 pass
- 3.1 | VERIFIED | `test/policy.test.mjs` 27 pass (password deny, download/payment/delete approval, off-allowlist read deny, ungranted observe deny)
- 3.2 | VERIFIED | `test/mask.test.mjs` 11 pass, including exact `DEFAULT_MAX_CHARS` truncation of a 30 000-char text
- 3.3 | VERIFIED | `test/approval.test.mjs` 10 pass (token, single use, argument binding, deny, unanswered, expiry)
- 3.4 | VERIFIED | `test/ratelimit.test.mjs` 5 pass, `test/audit.test.mjs` 6 pass
- 3.5 | VERIFIED | pipeline tests in `test/policy.test.mjs` (denied domain / ungranted tab zero driver calls, one inspect + one location before an approval-gated click, allowlist narrows / new task after last tab closes, unknown profile audited, rate_limited audited)
- 4.1 | VERIFIED | `chromiumArgs` runs without a process (flag list matches the committed report line for line); spawn/kill and daily-profile refusal in `test/chromium.test.mjs` (9 tests in the passing suite)
- 4.2 | VERIFIED | `spike a:` line present (grep count 1); `REPLAY_MODE='act'`, `TARGET_SOURCE='cdp-dom'` in `driver/session.mjs`; `scripts/spikes/generate-refusal.mjs` present
- 4.3 | VERIFIED | `spike c:` line present (1); `DEFAULT_REMOTE_ALLOW_ORIGINS` is the extension origin `chrome-extension://pljmheomijcfbgcbiccbbgehjckjnlij`; `doctor` prints the same value; `scripts/spikes/allow-origins.mjs` present
- 4.4 | VERIFIED | `test/observe.test.mjs` 10 pass, including the model-free links fixture, password kind on login.html, iframe with includeIframes, maxActions cap, live-vs-fixture count
- 4.5 | VERIFIED | `test/driver.test.mjs` end-to-end pass (open t1, observe, click by actionId to page2, back, type/read, PNG signature, tabs new/list/close, stale actionId `observe again`)
- 4.6 | VERIFIED | `test/generate.test.mjs` 11 pass (AWS key masked before refusal, `no model gateway configured`, task classes sensitive/browser-vision/browser-extract, extract by selectors vs instruction form)
- 4.7 | VERIFIED | driver tests for unsafe evaluate (approval_required, then `value: 2`, audit `unsafePayload`), unsafe cookies, unsafe cdp, all gated by token
- 5.1 | VERIFIED | `test/security.test.mjs` 10 pass, none skipped
- 5.2 | VERIFIED | `test/e2e.test.mjs` pass (one record per call, process gone, state file removed)
- 6.1 | VERIFIED | `test/network-audit.test.mjs` 5 pass; report has the `destination | port | via | count | first seen` table and no `Verdict:`
- 6.2 | VERIFIED | committed report `harness-core-network-20260911T130540Z.md` and a fresh HEAD re-run both show the harness process on loopback only (`node fetch`, `node net.connect`, `node websocket` all 127.0.0.1); `spike b:` line present (1). See gap 1 for a stale path inside that line
- 7.1 | VERIFIED | `plugin list` shows `claude: installed`, `codex: installed`, `setup: ok`; `~/.codex/config.toml` has exactly one `[mcp_servers.my-flow-browser]` with `startup_timeout_sec = 30`; the three Claude command lines are in the host-setup file. See gap 2
- 7.2 | VERIFIED | claude evidence file: four tool names, eleven audit lines, cross-checked against the real audit log (11 `claude-code` records, token `f7255b3d...` present twice); `Verdict:` 0; `e2e pending` 0
- 7.3 | VERIFIED | codex evidence file: four tool names, nine audit lines, cross-checked (9 `codex-mcp-client` records, token `1dcb2736...` present once); `Verdict:` 0; `e2e pending` 0
- 7.4 | VERIFIED | `grep -c browser_unsafe_evaluate README.md` 1; `grep -c "npm test"` 1 in CLAUDE.md and 1 in AGENTS.md
- 7.5 | VERIFIED | 3 spike lines, 15 test files in the script, suite ran with browser tests executing (0 skipped), `spec validate harness-core` exit 0
- 8.1, 8.2 | VERIFIED | full suite 173/173 at HEAD, Rebuild steps re-executed in this pass, diff scope clean (only ` M changes/harness-core/tasks.md`)
- 8.3 | VERIFIED | (a) `grep -c "no defect found"` on 8d is 1; (b) both `e2e pending` counts are 0, host files carry real evidence matching the audit log, `plugin list` shows claude and codex installed; this report is the fresh-context pass covering 004ad7c, 5cdd638 and 0f5976f, so a saved copy containing `Verdict: PASS` satisfies the final clause honestly

Delta spec scenarios (changes/harness-core/specs/browser-harness/spec.md, 38): all VERIFIED. Each maps to a test that ran in this pass's full `npm test` (173 pass, 0 fail, 0 skipped) or to the handshake pipe run:
tools/list is exact -> pipe run + server.test "publishes exactly the twelve tools" | Unknown tool and unknown argument refused -> pipe run (-32602) + server.test "unknown argument is refused rather than stripped" | In-flight responses not truncated -> pipe run 3 complete lines + server.test two shutdown tests | No browser after the handshake -> pipe run `spawned: 0`, no state file + server.test "starts no browser" | Shutdown kills the spawned browser -> e2e.test "leaves no browser behind" | Denied domain never reaches the driver -> policy.test "denied domain refuses with zero driver calls" | Ungranted tab makes no driver call -> policy.test "ungranted tab refuses with zero driver calls" | Only inspect and location run before the decision -> policy.test "classified click inspects exactly once" plus line 333 `driver.locations === 1` | A read makes no pre-decision call -> policy.test same title | Approved call does not act outside the allowlist -> regression.test "an approved call does not act on a page the task never had" | Approval precedes execution -> policy.test "approval runs the driver zero times before the token and once after" | New task after the last tab closes -> policy.test "the allowlist only narrows, and a new task starts after the last tab closes" | Rate limit is audited -> policy.test "over the rate limit the call is refused and recorded" + ratelimit.test | Allowed reads and clicks -> driver.test end-to-end + policy.test "read, screenshot and navigation are allowed" | Password typing denied -> security.test + driver.test | Download requires approval -> security.test "a download is not taken without approval, and leaves no file behind" | Payment and delete-like -> policy.test rows | Storage and cookies denied -> security.test "storage access is denied outright" + driver.test cookies only via unsafe + regression.test cookie scoping | Ungranted tab denied -> security.test "a popup is visible but ungranted" | Approved token executes once -> driver.test "runs once approved" + "token is single use" | Token bound to arguments -> driver.test + approval.test | Denied prompt -> approval.test + driver.test | Unanswered prompt -> approval.test "same token with less time left" | Password masked in read and extract -> security.test + generate.test | Secrets masked before the model callback -> generate.test "masked in the params before anything is sent" | Injection page yields data -> security.test "injected page text is returned as data and moves nothing" | Truncation is explicit -> mask.test asserts `text.length === DEFAULT_MAX_CHARS` and `truncated: true` | Click by candidate id -> driver.test end-to-end | Stale candidate refused -> driver.test `observe again` | Candidates from the fixture -> observe.test "model-free" | Password field marked -> observe.test login.html | Extract by instruction refuses -> generate.test | Task class set by the harness -> generate.test | Profile created under the my-flow home -> chromium.test spawn + e2e.test | Daily profile refused -> chromium.test "before starting a process" | Unknown profile refused -> policy.test "unknown profile reaches the gateway and is audited" | One record per call -> e2e.test 7 records + security.test "every call is on the record" | Unsafe payload audited, secrets never -> audit.test two tests + driver.test unsafe evaluate audit line

Main spec scenarios (specs/browser-harness/spec.md, 2): A headed launch shows its window | VERIFIED | fresh spike run under a temp home: `Chrome_WidgetWin_1|True|1280|800`, `main window visible: true`, exit 0, no chrome.exe left. Headless launches are unaffected | VERIFIED | full suite 173/173, 0 skipped at HEAD.

Design constraints: Do-Not-Touch | VERIFIED | no path outside this repo in 279a665..HEAD (17 paths) or in the whole history (92 paths); my-flow repo has no modified tracked file (only pre-existing untracked `changes/`, `specs/` dated before this change began); `changes/.templates/` untouched since the scaffold commit 049b10f; the only "User Data" references are the deny list at driver/chromium.mjs:117-119. Rebuild / Re-run | VERIFIED | `npm test` at HEAD in this pass; network audit re-run at HEAD in this pass with the same loopback-only harness result as the committed copy; `spec validate` exit 0; `### Spike results` holds the three lines. Fake-completion grep | VERIFIED | two benign hits (the deliberate fail-closed `not implemented until sub-change 4` refusal in llm/generate.mjs and its test; `return null` only as parse results). Package sanity | VERIFIED | `npm ls --depth=0` clean with the three exact pins, 15 test files all in the script.

### Blockers and gaps

None blocking. Two documentation stalenesses, neither a code defect and neither contradicting a task's acceptance text:
1. The `spike b:` line in `changes/harness-core/design.md` names the report copy as `.my-flow/verify/harness-core-network-20260910T212609Z.md`, but only the 092445Z and 130540Z copies exist under `.my-flow/verify/` now (the original still exists under `~/.my-flow/browser/audit/network-2026-09-10T21-26-09-457Z.md`). `.my-flow/` is disposable scratch, and the destination set the line describes matches both current copies and the fresh run.
2. `.my-flow/verify/harness-core-host-setup.md` still contains the sentence `install codex pending: user confirmation`, written before the user ran the real install. Task 7.1's acceptance is an either/or and both halves are individually true today. A one-line update of that file would remove the contradiction.

Known and accepted by decision, not counted against this change: Chromium's own background connections to Google hosts (recorded in the `spike b:` line and reproduced in the fresh audit; the harness process itself reaches loopback only), and the frame-enumeration completeness question, filed as `changes/request-interception/`.

### Recommendation

Verdict: PASS. Save this report as `.my-flow/verify/harness-core-<timestamp>.md`, tick 8.3, and run `/my-flow:spec archive harness-core`. Optionally fix the two stale sentences in gaps 1 and 2 before archiving; they do not need a re-verify.
