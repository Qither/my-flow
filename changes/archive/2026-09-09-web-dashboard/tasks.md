## 1. Shared intent library

- [x] 1.1 Create `scripts/lib/intent.mjs` holding `read`, `listChanges`, `tasks`, `artifactState`, `newestMtime`, `overlapIndex`, `auditNudges`, `validateSpecFile`, `validateChange`, `validateDeltaAgainstMain` and `setState` taking `root` as their first argument, the pure helpers `norm`, `today`, `overlapText`, `splitRequirements` and `sectionBody` with their present signatures, `numOpt` taking the parsed options object as an argument since `scripts/spec.mjs:229` calls it with a null flag name, and a new `readState(root)` replacing the inline parses at `scripts/spec.mjs:190-196` and `:477`, with the overlap index computed per call instead of the module cache at `scripts/spec.mjs:106`, and verify `node -e "import('./scripts/lib/intent.mjs').then(m => console.log(Object.keys(m).length, Object.keys(m).sort().join(',')))"` prints eighteen names and exits 0
- [x] 1.2 Add `statusReport(root, opts)` and `validateReport(root, opts)` returning `{ json, text }` built from the expressions at `scripts/spec.mjs:200-242` and `:363-376` moved unedited, resolving `staleDays` and `auditEvery` through `numOpt` when the caller passes neither, and verify a node one-liner shows `statusReport` reporting the `web-dashboard` row with keys `root,current,changes,warnings`, and shows `MY_FLOW_STALE_DAYS=1` and `--stale-days`-equivalent option values changing the stale marking exactly as the CLI does
- [x] 1.3 Confirm the library never terminates its caller, per `changes/web-dashboard/specs/spec-helper/spec.md:35`, and verify a single node process calls `statusReport` and `validateReport` for a root with no `changes/` directory and for a change name that does not exist, prints all four results, and exits 0 of its own accord
- [x] 1.4 Rewrite `scripts/spec.mjs` to import every moved helper from the library, keeping `new`, `archive`, `abandon` and `stage` local and reducing `status` and `validate` to `out(r.json, r.text)` plus the exit code, and verify `npm test` passes with `test/spec.test.mjs` and `test/helpers.mjs` unmodified (`git diff --stat test/` is empty)
- [x] 1.5 Capture byte-identity of the CLI surface against the pre-refactor baseline by extracting `git show HEAD:scripts/spec.mjs` into a temp copy under `scripts/` (so its relative `templates/` path resolves), running `status`, `status --json`, `validate` and `validate --json` from both the temp copy and the current `scripts/spec.mjs` against this repository, then deleting the temp copy, and verify all four `diff` runs report no differences and both exit codes match per command

## 2. Dashboard server and JSON API

- [x] 2.1 Create `scripts/dashboard.mjs` exporting `startServer({ root, port, host })` that normalizes the root once with `realpathSync.native(resolve(root))`, binds loopback, resolves `web/` from the script's own directory, and returns `{ url, port, server, close }` whose `close` ends the event streams and calls both `server.close()` and `server.closeAllConnections()`, and verify a node one-liner starts it on `port: 0`, fetches `/api/health`, prints a `pid` equal to `process.pid`, and exits within 2 s after `close()` with a prior keep-alive fetch still open
- [x] 2.2 Write `.my-flow/state/dashboard.json` with `pid`, `host`, `port`, `url`, `root` and `started` after `listen` resolves, remove it on `close()` and on `SIGINT` / `SIGTERM`, and verify the file exists with the bound port and the normalized root while the server runs and is gone after `close()`
- [x] 2.3 Implement `GET /api/status` and `GET /api/validate` serving the library's `json` value unwrapped, and verify a new test in `test/dashboard.test.mjs` deep-equals each response against `runSpecJson(root, ['status'])` and `runSpecJson(root, ['validate'])` for the same fixture project
- [x] 2.4 Implement `GET /api/changes/<name>`, `/api/specs`, `/api/archive` and `/api/scratch` per the design's route table, including `<!-- via: -->` extraction and the archived / abandoned split, and verify tests assert the fixture's delta capability appears under the change, the `via` marker is returned for `specs/cap`, and the two archive entries carry `kind` `archived` and `abandoned`
- [x] 2.5 Wrap every handler in one `try`/`catch` returning 500 with the standard error body, since `newestMtime` (`scripts/spec.mjs:91-98`) throws `ENOENT` when a file vanishes between `readdirSync` and `statSync` as `spec archive` renames a change directory (`:474`), and verify a test that deletes a change directory concurrently with repeated `/api/status` requests never yields an unhandled rejection and every response is either 200 or a 500 carrying `{ ok: false, error }`
- [x] 2.6 Implement the static handler for `web/` with resolved-path containment, `index.html` fallback for extensionless paths, and a 404 for unknown `/api/` routes, and verify tests show `GET /../package.json` and `GET /%2e%2e/package.json` return 404 while `GET /` returns the shell HTML

## 3. Push updates

- [x] 3.1 Implement `GET /api/events` as SSE emitting `hello` on connect, debounced `change` events, and a 25 s `: ping` comment, normalizing each watcher `filename` by joining it with its watched directory name, converting platform separators to `/`, and coalescing a `null` filename to that directory, and verify a test appends a line to `changes/demo/tasks.md` and receives a `change` event whose payload contains exactly `changes/demo/tasks.md` with no backslash, within 2 s
- [x] 3.2 Watch `specs/`, `changes/` and `.my-flow/` with `fs.watch({ recursive: true })` guarded by try/catch and an `error` listener per watcher, filter out `.my-flow/state/dashboard.json`, and fall back to a 2 s `newestMtime` poll when a watcher throws or errors, noting Linux recursive watch needs Node 20.13.0, and verify a test writing only `.my-flow/state/dashboard.json` produces no `change` event within 1 s while a write to `specs/cap/spec.md` does, and that a forced watcher error leaves the server alive and still emitting events

## 4. Save guard

- [x] 4.1 Implement `GET /api/file?path=` with the read allow-list (`specs/**`, `changes/**`, `.my-flow/{ask,verify,interviews}/**`, `.md` only) returning `{ path, content, mtimeMs, eol, writable }`, and verify tests show `package.json` returns 403 and `.my-flow/ask/x.md` returns 200 with `writable: false`
- [x] 4.2 Implement `POST /api/file` with the ordered guard from design D7 (string path, not absolute, root-contained, `.md`, write allow-list from `listChanges`, file must exist), and verify tests return 400 for `../escape.md`, an absolute path, and `C:foo.md` whose `isAbsolute` is false yet whose `resolve` escapes the root, with no file created outside the root, 403 for `changes/archive/2026-01-01-old/proposal.md`, `.my-flow/ask/x.md` and `changes/demo/notes.txt`, and 404 for `changes/demo/ghost.md`
- [x] 4.3 Implement the same-origin guard from design D5a (Host must be loopback with the bound port, any `Origin` must match the server's own, `POST /api/file` requires `application/json`, body capped at 5 MB, no CORS headers emitted), and verify tests show a POST with `Host: evil.example` refused 403, a POST with `content-type: text/plain` refused 415, an over-size body refused 413, and the target file byte-identical after all three
- [x] 4.4 Implement the mtime check returning 409 with `error: "mtime"` and the current on-disk content, bypassed by `force: true`, and verify tests show a stale `mtimeMs` leaves the file byte-identical while the same request with `force: true` returns 200 and changes it
- [x] 4.5 Implement line-ending preservation by normalizing incoming content to LF and converting to CRLF when the on-disk file contains CRLF, and verify tests show a CRLF fixture keeps `\r\n` with no lone `\n` and an LF fixture contains no `\r` after a save

## 5. Frontend

- [x] 5.1 Write `web/md.mjs` exporting a DOM-free `renderMarkdown` covering the design D8 subset with escape-before-markup, hiding HTML comments while surfacing `<!-- via: <date>-<name> -->` as a link to the matching `changes/archive/` entry, and verify `test/markdown.test.mjs` asserts a fixed expected HTML string per construct, a golden render of `templates/change/design.md`, and a requirement carrying a via-marker rendering that link with no other comment present in the output
- [x] 5.2 Assert the renderer's safety and fallback behavior, including the rule that a bare relative link target carries no colon before its first slash, and verify `test/markdown.test.mjs` shows `<script>alert(1)</script>` rendered escaped and `javascript:`, `data:text/html` and `vbscript:` targets producing no `href`, while a blockquote and an image render as escaped plain text
- [x] 5.3 Write `web/index.html` and `web/app.css` with the paper / ink / lime tokens, square corners, 2-3 px borders, hard offset shadows, monospace metadata, 120 ms transitions and a `prefers-color-scheme: dark` block, taken from `../my-flow-sample/src/styles/globals.css:21-45` and `:86-99`, and verify a test reads all four files under `web/`, asserts no `http://` or `https://` occurrence anywhere, and fetches of `/`, `/app.mjs`, `/app.css` and `/md.mjs` return 200 with the expected content types
- [x] 5.4 Write `web/app.mjs` with the hash router, the four pages, the editor with Save / Reload / Overwrite, the echo suppression that ignores a `change` event matching the mtime of the last successful save, and the rule that every `hello` including a reconnect triggers a full refetch, and verify `node --check web/app.mjs` exits 0 and a node one-liner imports `web/app.mjs` with a minimal `globalThis.document` / `window` / `EventSource` stub without throwing; the router and editor behavior itself is exercised in the browser under task 9.2, so this box stays unticked until 9.2 has passed
- [x] 5.5 Add the `test` script entry listing all four suites in `package.json` with no `dependencies` key added, and verify `npm test` runs `spec`, `completion-guard`, `dashboard` and `markdown` suites and reports zero failures

## 6. CLI subcommands

- [x] 6.1 Add `dashboard: 'dashboard.mjs'` to the `scripts` map and one help line to `scripts/cli.mjs`, and verify `node scripts/cli.mjs --help` lists the dashboard command and `node scripts/cli.mjs dashboard status --json` exits 0 with a parseable JSON object
- [x] 6.2 Implement `dashboard start` detaching a `serve` child with `detached: true`, `stdio: 'ignore'`, `windowsHide: true` and `unref()`, polling up to 5 s for the state file, plus `--foreground`, `--port` and `--root` with the root normalized as in 2.1, and verify a start prints a `http://127.0.0.1:<port>/` URL, the parent exits immediately, a fetch of that URL returns 200, a second start on the same port exits non-zero naming the port and writes no state file, and the run ends with `dashboard stop` leaving no server process and no state file
- [x] 6.3 Implement `dashboard stop` and `dashboard status` with the identity check from design D3 (`Number.isInteger(pid) && pid > 0` before any `process.kill`, `ESRCH` distinguished from `EPERM`, `/api/health` pid and root compared case-insensitively on win32 before any signal, a disagreeing probe deleting the file and a failed probe keeping it), and verify a state file naming a dead pid makes `status` report stale and delete it, a state file whose port answers with a different root deletes it without signalling, and a state file whose port answers nothing is reported and left on disk

## 7. Skill and build

- [x] 7.1 Write `src/skills/dashboard.md` in the shape of `src/skills/spec.md` covering `start | stop | status`, using the `MY-FLOW:CLAUDE` and `MY-FLOW:CODEX` blocks for the two script paths, and add a `dashboard` entry to `manifest.json`'s `skills` map, and verify `npm run build` reports writing `skills/dashboard/SKILL.md`, `codex/skills/my-flow-dashboard/SKILL.md` and `.claude-plugin/plugin.json`
- [x] 7.2 Confirm the build touched no other generated file, and verify `npm run check` prints `generated files are up to date` and `git status --short` lists only the three new or regenerated outputs alongside the intended source edits

## 8. Documentation

- [x] 8.1 Add a `## Dashboard` section to `README.md` after `## Cross-model advisor`, covering the launch command, the four pages, editing with the mtime guard, and the loopback-only constraint, plus the TOC entry with items 10-14 renumbered, a `dashboard` row in the CLI commands table and a `dashboard` row in the Skills table, and verify `grep -c "dashboard" README.md` is at least 4 and the TOC lists fifteen numbered entries
- [x] 8.2 Translate the same section, TOC entry and two table rows into `README.de.md`, `README.es.md`, `README.fr.md`, `README.ja.md`, `README.ko.md`, `README.zh-CN.md` and `README.zh-TW.md`, keeping code blocks identical across languages, and verify each of the eight files contains the launch command line and a TOC with fifteen numbered entries

## 9. End-to-end evidence

- [x] 9.1 Run the dashboard against this repository and tick a box in `changes/web-dashboard/tasks.md` from the terminal while a node `EventSource`-equivalent client reads `/api/events`, and verify a transcript under `.my-flow/verify/web-dashboard-e2e-<time>.md` records the raw `change` event with its timestamp less than one second after the write, alongside a browser screenshot showing the updated progress count without a reload
- [x] 9.2 Drive the browser through all four pages against `../my-flow-sample` or this repository, saving `proposal.md` from the editor and exercising the Reload and Overwrite paths after an outside edit, and verify the same transcript records screenshots of each page, the edited content on disk with its original line endings unchanged, no duplicate "changed on disk" warning after the client's own save, and a start-and-stop cycle driven through the `/my-flow:dashboard` skill path
- [x] 9.3 Confirm the pages are usable with the machine's network disabled, per `changes/web-dashboard/specs/dashboard/spec.md:193`, and verify the transcript records all four pages rendering, navigating and receiving a push update with networking off, and the browser devtools network panel showing no failed external request

## 10. Final gate

- [x] 10.1 Targeted verification of the whole change (`npm run build`, `npm run check`, `npm test`, and the Rebuild / Re-run steps from design.md) and verify every command exits 0 with output pasted into the session
- [x] 10.2 Cleanup of own diff only, then re-verify, and verify `npm test` and `npm run check` still pass after the cleanup and the diff contains no debug logging, dead code or Do-Not-Touch files
- [x] 10.3 Independent verification via `/my-flow:mf-verify web-dashboard` and verify a report under `.my-flow/verify/` records `Verdict: PASS`
- [x] 10.4 Declare done and set the stage, and verify `node scripts/spec.mjs stage web-dashboard done` prints the new stage and `node scripts/spec.mjs status` shows every box ticked
