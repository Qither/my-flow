## 1. Delta specs (the contract this change must satisfy)

The two ADDED-only deltas at `changes/intent-layer-gc/specs/spec-helper/spec.md` and
`changes/intent-layer-gc/specs/mf-audit/spec.md` were written during planning and are the
contract every group below implements. Read them before starting; do not rewrite them to match
whatever the code ends up doing.

- [x] 1.1 Read both delta specs end to end before writing any code and treat their scenarios as the acceptance criteria for groups 2 to 8, in particular the exact output strings they quote (`[stale 30d]`, `overlap: cap "Foo" in changes a (MODIFIED) and b (REMOVED)`, `audit suggested: cap (5 merges since last audit)`), and verify `node scripts/spec.mjs validate intent-layer-gc` exits 0 with no error and `node scripts/spec.mjs status intent-layer-gc` reports `2 delta spec(s)`

## 2. Option parsing and stale detection

- [x] 2.1 In `scripts/spec.mjs`, extend the flag loop at line 31 so `--stale-days` and `--reason` consume their following argument the way `--root` does, and add a `numOpt(flag, envVar, fallback)` helper that resolves CLI flag then environment variable then default and ignores non-numeric or non-positive values, and verify that in a temp root where `spec new demo` has been run first, `node scripts/spec.mjs status --stale-days 30 --root <tmp>` prints the `demo` row and no longer prints `30: (missing)`, the pre-existing change being what stops an empty root from passing the check through the `no active changes` message
- [x] 2.2 Add `newestMtime(dir)` to `scripts/spec.mjs`, a recursive walk over `readdirSync(dir, { withFileTypes: true })` plus `statSync` returning the maximum `mtimeMs` (do not use the `recursive` option of `readdirSync`; it postdates Node 20.0), and give each `status` row `stale` and `lastModified`, rendering ` [stale <n>d]` after the row text when the change is stale, using an interim hardcoded threshold of 14 days that task 2.3 makes configurable, and verify a new test in `test/spec.test.mjs` that back-dates a change 30 days with `utimesSync` asserts the text mark and the JSON fields while a second, fresh change carries neither
- [x] 2.3 Wire the stale threshold to `--stale-days` and `MY_FLOW_STALE_DAYS` with default 14, and suppress the stale mark when a change's tasks are all ticked, and verify a test asserts that `--stale-days 60` and `MY_FLOW_STALE_DAYS=60` each clear the mark on the 30-day-old change, and that ticking every box clears it at the default threshold
- [x] 2.4 Add the state-file warning to `status`: when `.my-flow/state/current-change.json` names a change that is stale or no longer exists, append a warning line to the text output and an entry to a new top-level `warnings` array in the JSON, suppressed when the recorded stage is `archived`, and verify a test covers all three cases (stale current change warns, missing current change warns, stage `archived` after a successful `spec archive` does not warn)

## 3. Delta-overlap detection

- [x] 3.1 Add a memoized `overlapIndex()` to `scripts/spec.mjs` that reads every active change's `changes/<n>/specs/<cap>/spec.md` and records, per capability and requirement name, each claim as the change name plus its section kind: ADDED, MODIFIED and REMOVED names come from `splitRequirements(sectionBody(...))`, while RENAMED names come from reusing the `- FROM: ### Requirement:` regex at `scripts/spec.mjs:242` because `splitRequirements` at line 270 matches only `### Requirement:` headings and never sees a FROM line; return only the entries claimed by two or more changes, and print one `status` warning per entry in the form `overlap: <cap> "<Req>" in changes <a> (MODIFIED) and <b> (REMOVED)`, and verify a test with two active changes claiming `Foo` in `specs/cap/spec.md`, one under MODIFIED and one under REMOVED, asserts the warning names both changes with their section kinds, and that a single-claimant requirement produces no warning
- [x] 3.2 Use the same `overlapIndex()` in `validateChange(n)` to attach, as warnings only and never errors, the entries that involve `n`, and verify a test asserts `node scripts/spec.mjs validate <one-of-the-two>` still exits 0 and prints a `warn:` line naming the other change and its section kind
- [x] 3.3 Add a test that a RENAMED delta whose `- FROM:` line names a requirement another active change carries under MODIFIED produces an overlap warning showing kinds `RENAMED` and `MODIFIED`, and verify `node --test test/spec.test.mjs` passes it, proving the FROM regex path is wired and not silently dropped

## 4. The `abandon` subcommand

- [x] 4.1 Extract `moveToArchive(name, suffix)` from the tail of the `archive` block at `scripts/spec.mjs:340-349` (create `changes/archive/<date>-<name><suffix>`, `renameSync`, set the state to `archived` only when the moved change was current) and call it from `archive` with an empty suffix, changing no behavior, and verify `node --test test/spec.test.mjs` still passes all existing archive and stage tests unchanged
- [x] 4.2 Add the `abandon` subcommand to `scripts/spec.mjs` with its gates in order: the change must exist; `--reason "..."` appends an `## Abandoned` section and a `**Reason**:` line to `proposal.md` when absent; without an `## Abandoned` section carrying a `**Reason**:` line the command fails with exit 1; a change with every task ticked is refused unless `--force`, and verify a test asserts exit 1 and the message for the missing-reason case, exit 1 for the fully-ticked case, and exit 0 for the fully-ticked case with `--force`
- [x] 4.3 Complete `abandon` by calling `moveToArchive(name, '-abandoned')` and never reading or merging delta specs, and verify a test asserts the directory lands at `changes/archive/<date>-<name>-abandoned/`, that `specs/cap/spec.md` is byte-identical before and after even though the change carried an ADDED delta, and that the state becomes `archived` when the abandoned change was current and is untouched when another change was current
- [x] 4.4 Update the usage line at `scripts/spec.mjs:366` and the header doc comment at lines 5-9 to list `abandon <name> [--reason "..."] [--force]` and the `--stale-days <n>` option, and verify `node scripts/spec.mjs` with no arguments in a directory without `changes/` prints the usage string containing `abandon` and exits 1
  - note: with no arguments spec.mjs runs `status` by existing design (`cmd === undefined`), so the usage string was verified through an unknown subcommand (`spec.mjs bogus`), which exits 1 and names `abandon` and `--stale-days`.

## 5. Provenance marker on merge

- [x] 5.1 Give `mergeDelta` a fourth parameter `via` (the string `<date>-<name>`) and stamp every ADDED and MODIFIED block before `blocks.set` by first deleting every via-marker line anywhere in the block, not only the line after the heading, then inserting one marker directly after the `### Requirement:` heading, passing `` `${today()}-${name}` `` from the `archive` block, and verify a test archives a change with one ADDED and one MODIFIED requirement and asserts each merged block contains exactly one `<!-- via: ` line, placed on the line directly after its heading
- [x] 5.2 Add a test that archives a second change whose MODIFIED delta block already contains a literal `<!-- via: 2020-01-01-old -->` line placed several lines below the heading, in the middle of the requirement text, and asserts the merged block afterwards contains exactly one via-marker, that it names the second change and not `2020-01-01-old`, and that it sits directly after the heading, and verify `node --test test/spec.test.mjs` passes it and `node scripts/spec.mjs validate` exits 0 on the resulting `specs/` tree

## 6. Audit nudge in `status`

- [x] 6.1 Add the nudge to `status`: guard the archive read with `existsSync(join(CHANGES, 'archive'))` because the directory is created only by `scripts/init.mjs:53` and is absent from this repository, so an unguarded `readdirSync` throws ENOENT on any project that has `specs/` but has never archived; then, for each capability directory under `specs/` whose name matches `[a-z0-9][a-z0-9-]*`, sort the archive lexicographically, find the last directory matching the anchored pattern `^\d{4}-\d{2}-\d{2}-(?:.*-)?audit-<cap>(?:-abandoned)?$`, count the directories after it that contain `specs/<cap>/spec.md` and do not end in `-abandoned`, and when the count reaches `MY_FLOW_AUDIT_EVERY` (default 5, resolved through the same `numOpt` helper) append `audit suggested: <cap> (<count> merges since last audit)` to the text warnings and to the JSON `warnings` array, and verify a test with five archived directories each carrying `specs/cap/spec.md` asserts the line appears while four produce none
- [x] 6.2 Add tests for the four nudge edge cases: the count resets after an archived directory named `<date>-something-audit-cap`; a directory ending in `-abandoned` is not counted as a merge even when it carries `specs/cap/spec.md`; a project with `specs/` but no `changes/archive/` directory exits 0 with no nudge and no ENOENT; and a project with no `specs/` produces no nudge, and verify `node --test test/spec.test.mjs` passes all four
- [x] 6.3 Add a test for the anchor's false-positive case: with a capability directory `specs/spec/` and an archived directory `<date>-audit-spec-helper`, assert that the counter for capability `spec` is NOT reset, because a substring test for `-audit-spec` would wrongly match, and verify the same fixture does reset the counter for a capability directory named `spec-helper`
- [x] 6.4 Add a test for the clean-audit escape hatch from design.md D-8: after `spec new audit-cap` followed by `spec abandon audit-cap --reason "audit clean, no findings"`, assert the archived directory `<date>-audit-cap-abandoned` is recognised as an audit marker and clears the nudge for capability `cap`, and verify the same directory is still excluded from the merge count, proving the deliberate asymmetry between the two uses of the `-abandoned` suffix

## 7. The `mf-audit` skill

- [x] 7.1 Write `src/skills/mf-audit.md` following the house style of `src/skills/mf-verify.md`: frontmatter with `name`, `description`, `argument-hint: "<capability | all>"`; an `Input: {{ARGS}}` line; the five numbered steps and the seven-section report template from design.md D-10; the `<!-- MY-FLOW:CLAUDE -->` block naming `subagent_type: my-flow:architect` and the `<!-- MY-FLOW:CODEX -->` block naming the native `architect` subagent; and the hard rules that it never edits `specs/` or `changes/archive/`, that the suggested change name ends in `audit-<cap>`, and that a CLEAN status is closed with the two-command clean-audit record from D-8, and verify the file contains both conditional blocks and all seven report sections
- [x] 7.2 In the same file, make the loop override explicit rather than an output-format note: the delegation prompt must state that for this run steps 1 and 2 of the architect loop at `src/agents/architect.md:25-26` (read proposal.md and design.md) are replaced by reading `specs/<cap>/spec.md` and opening the source and test files each requirement would touch, that the report template replaces the output block at `src/agents/architect.md:33` in full including its `## Verdict:` line, and that the rest of the role's contract still applies; add step 4's deterministic guard instructing the skill's own context to scan the returned text for any line matching `Verdict:` and rewrite it to the `### Status:` form or send it back for one correction round before writing the file, and verify `grep -n "Verdict" src/skills/mf-audit.md` shows matches only inside the override sentence and the rewrite rule, never inside the report template
- [x] 7.3 Register `mf-audit` in `manifest.json` under `skills` with `"claude": { "allowed-tools": "Read Grep Glob Bash Agent Write" }`, then run `npm run build`, and verify `npm run check` prints `generated files are up to date` and `skills/mf-audit/SKILL.md`, `codex/skills/my-flow-mf-audit/SKILL.md` and the `skills` array in `.claude-plugin/plugin.json` all exist and name the skill
- [x] 7.4 Run `mf-audit` end to end against a throwaway fixture, since `proposal.md:128` names `my-flow-sample`, which does not exist in this repository: build a fixture root under the scratchpad directory (never inside the repository, per design.md Do-Not-Touch) containing a hand-written `specs/demo-cap/spec.md` with three requirements, one of them deliberately describing behavior the fixture code lacks, plus a small `src/` and `test/` implementing the other two; record `sha256` of every file under the fixture's `specs/`, then point the skill at the fixture by making it the working directory, since `mf-audit` reads `specs/` relative to the current directory and takes no `--root` argument, so `cd` into the fixture root before invoking `/my-flow:mf-audit demo-cap`, or open a Claude Code session there, and verify a report exists at `.my-flow/verify/audit-demo-cap-<timestamp>.md`, that it carries a `### Status:` line and no line matching `Verdict:`, that it names the unimplemented requirement under `### Unimplemented requirements`, that its last section is a suggested change name ending in `audit-demo-cap`, and that re-hashing the fixture's `specs/` reproduces the recorded digests byte for byte

## 8. Documentation and generated blocks

- [x] 8.1 Update `src/skills/spec.md`: add an `## abandon <name>` section that also documents the clean-audit record from design.md D-8 as its second use, extend `## status [name]` with the stale mark, the state warning, the overlap warning and the audit nudge, extend `## validate [name]` with the overlap warning, and note the provenance marker under `## archive <name>`; update the `argument-hint` to include `abandon`, then run `npm run build`, and verify `npm run check` passes and `skills/spec/SKILL.md` contains the `abandon` section
- [x] 8.2 Update `src/core/core.md` section 3 line 47 to read `new / status / validate / abandon / archive / stage` and section 2 line 39 to list `{{CALL:mf-audit}}` among the skills, then run `npm run build`, and verify `npm run check` passes and both `claude/CLAUDE.block.md` and `codex/AGENTS.block.md` contain `abandon` and the tool-appropriate `mf-audit` reference
- [x] 8.3 Update `templates/specs-README.md` (the subcommand list on line 4, the archive line in the layout block for `-abandoned`, and one paragraph on the provenance marker), `templates/project/CLAUDE.md` line 8 and `templates/project/AGENTS.md` line 7 to list the `mf-audit` skill, and `templates/project/rules/specs.md` line 19 to say that abandoned changes never merge, and verify `grep -rn "abandon" templates/` returns a hit in each of the four files
- [x] 8.4 Add to all eight README files (`README.md` and the de, es, fr, ja, ko, zh-CN, zh-TW translations, which share identical line numbering) four minimal edits, translated in kind: one `spec abandon` row after the `spec archive` row at line 136, one clause in the `spec status` row at line 134 for stale plus overlap plus nudge, one `mf-audit` row in the skills table after the `mf-verify` row, and `abandon` added to the skills-table `spec` row at line 152 whose first cell currently reads `spec new|status|validate|archive|stage`, and verify `grep -c "spec abandon" README*.md` reports at least 1 for each of the eight files and `grep -n "validate\\\\|abandon" README*.md` shows the updated skills-table row in all eight

## 9. Final gate

- [x] 9.1 Reconcile the two delta specs with the behavior actually implemented, correcting a scenario only where implementation proved the written contract wrong and listing each such correction in the final report, and verify every exact output string the scenarios quote appears verbatim in a passing test in `test/spec.test.mjs` and `node scripts/spec.mjs validate intent-layer-gc` still exits 0
- [x] 9.2 Targeted verification of the whole change: run `npm run check`, `npm test`, and `node scripts/spec.mjs validate intent-layer-gc`, and verify all three exit 0, `npm test` reports strictly more than the 31 tests that passed before this change with 0 failures, and `git status --short` lists nothing under `specs/` or `hooks/`
- [x] 9.3 Cleanup of own diff only (no drive-by edits, no leftover scratch files, no `test.skip` or `.only`, no placeholder TODO), then re-verify by re-running the three commands from task 9.2, and verify `git diff --stat` touches only `scripts/spec.mjs`, `test/spec.test.mjs`, `src/`, `manifest.json`, `templates/`, `README*.md`, the generated outputs, and `changes/intent-layer-gc/`
- [x] 9.4 Independent verification via `/my-flow:mf-verify intent-layer-gc` against the proposal's Success Criteria, and verify the report written under `.my-flow/verify/` names the change and says `Verdict: PASS`
