## 1. Scrollbar styles in web/app.css

- [x] 1.1 Snapshot the starting state first with `git status --short > "$SCRATCH/status-before.txt"`, `cp web/app.css "$SCRATCH/app.css.before"`, `cp -r web "$SCRATCH/web.before"` and `cp -r test "$SCRATCH/test.before"` where `$SCRATCH` is this session's scratchpad directory (all of `web/` and the two new test suites are untracked, so these copies are the only baseline), then add `--sb-track-color: #ffffff` and `--sb-thumb-color: #000000` to the existing `:root` block in `web/app.css:3-23`, and `--sb-track-color: #1f1f1f` and `--sb-thumb-color: #e6e6e6` to the existing `@media (prefers-color-scheme: dark) { :root { ... } }` block at `web/app.css:24-34`, per design D1 and `../my-flow-sample/src/styles/globals.css:290-300`, changing no other declaration, and verify `grep -n -- "--sb-" web/app.css` prints exactly four lines, two inside each block, and a saved pre-edit copy of the file (`web/app.css` is untracked, so plain `git diff` shows nothing for it) diffed with `diff -u "$SCRATCH/app.css.before" web/app.css` shows only additions in that region
- [x] 1.2 Append the `/* scrollbars */` block at the end of `web/app.css` after `.highlight` (`web/app.css:174`) implementing design D2 to D6 exactly: `body::-webkit-scrollbar` 20 px with a track carrying `border-left: 4px solid #000`, a `@media (prefers-color-scheme: dark)` rule giving `body::-webkit-scrollbar-thumb` the same 4 px black left border, `.editor textarea` and `.sidebar` at 16 px with no border plus `.editor textarea::-webkit-scrollbar-corner` filled with the track colour, `pre` at 10 px on both axes with `border: 0`, a transparent track and corner and a `var(--sb-thumb-color)` thumb, all tracks and thumbs painted from the two custom properties, and the `@supports not selector(::-webkit-scrollbar)` fallback setting `scrollbar-color` and `scrollbar-width`, and verify `grep -o -- "::-webkit-scrollbar" web/app.css | wc -l` reports at least sixteen, the count the snippets in design D2 to D6 spell out, `grep -n "@supports not selector" web/app.css` prints exactly one line, and a grep for each of `body::-webkit-scrollbar-track`, `body::-webkit-scrollbar-thumb`, `.editor textarea::-webkit-scrollbar-corner`, `.sidebar::-webkit-scrollbar` and `pre::-webkit-scrollbar-corner` finds it

## 2. Static and suite verification

- [x] 2.1 Check the stylesheet still parses and stays offline by running a node one-liner that reads `web/app.css` and asserts its `{` and `}` counts are equal and that no `url(`, `@import` or `http` substring occurs, then starting the dashboard and fetching `/app.css`, and verify the one-liner prints equal brace counts and exits 0, `grep -rE "https?://" web/` prints nothing and exits 1, and the fetch returns status 200 with a `text/css` content type
- [x] 2.2 Run the full suite with `npm test` from the repository root, and verify it reports 70 passing tests and 0 failing, with every file under `test/` unmodified, checked against the pre-edit snapshot from task 1.1 (`git status --short` shows no new entry under `test/` and the two untracked suites `test/dashboard.test.mjs` and `test/markdown.test.mjs` are byte-identical to copies taken alongside `app.css.before`; plain `git diff --stat test/` proves nothing for untracked files)

## 3. Visual evidence

- [x] 3.1 Start the dashboard on this repository and drive headless Chrome over the DevTools protocol as `.my-flow/verify/web-dashboard-e2e-shots/` was produced (a script kept in the scratchpad, `Page.captureScreenshot`), forcing the theme explicitly for both passes with `Emulation.setEmulatedMedia` and `features: [{name: 'prefers-color-scheme', value: 'light'}]` then `value: 'dark'` rather than trusting the headless default, launching Chrome without `--hide-scrollbars` and capturing with `captureBeyondViewport` left false since either one removes the bars from the image, and opening the editor on `changes/dashboard-scrollbar/design.md`, whose fenced code lines exceed 100 characters so the rendered `pre` scrolls horizontally, and on `changes/dashboard-scrollbar/tasks.md`, whose long task lines overflow the textarea vertically, writing the images under `.my-flow/verify/dashboard-scrollbar-shots/` and a note at `.my-flow/verify/dashboard-scrollbar-<UTC time>.md` listing each image with what it shows, and verify the note exists, at least four images were written covering light and dark, and each screenshot shows flat square bars at the design's widths with a black-bordered page track and no rounded translucent default bar

## 4. Change scope

- [x] 4.1 Confirm the change touched nothing else by running `git status --short` and `git diff --stat`, and verify `git status --short` shows no new path compared to the snapshot taken in task 1.1 other than under `changes/dashboard-scrollbar/` (the pre-existing unrelated modified and untracked paths are out of scope, and `.my-flow/` is git-ignored so the screenshot evidence never appears), and, because `git status` collapses the untracked tree to one `?? web/` line, `diff -rq "$SCRATCH/web.before" web` names `app.css` as the only differing file and `diff -rq "$SCRATCH/test.before" test` prints nothing

## 5. Final gate

- [x] 5.1 Targeted verification of the whole change: re-run `npm test`, the `grep -rE "https?://" web/` check and a fresh light-mode screenshot after any fix made since task 3.1, and verify all three pass in one uninterrupted pass with output captured
- [x] 5.2 Cleanup of the change's own diff only, removing any leftover debug rule, commented-out declaration or stray blank line introduced by tasks 1.1 and 1.2, then re-verify by re-running `npm test` and re-reading the appended block, and verify the suite is still 70/70 and `diff -u "$SCRATCH/app.css.before" web/app.css` (the file is untracked, so `git diff` is vacuous) contains no commented-out or duplicated declaration
- [x] 5.3 Independent review in a fresh context via `/my-flow:mf-verify dashboard-scrollbar`, and verify the report written under `.my-flow/verify/` contains the line `Verdict: PASS`
- [x] 5.4 Declare the change done by running `node scripts/spec.mjs stage dashboard-scrollbar done`, and verify the command prints `dashboard-scrollbar: stage done` and `node scripts/spec.mjs status dashboard-scrollbar` shows every task ticked
