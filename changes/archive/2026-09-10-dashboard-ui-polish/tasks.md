## 1. Baseline snapshot and the server's per-file modification time

- [x] 1.1 Take the baseline this change is measured against, because `web/`, `test/diff.test.mjs`, `scripts/dashboard.mjs` and `src/skills/dashboard.md` are all untracked and `git diff` says nothing about them (design, driver 2): with `$SCRATCH` set to this session's scratchpad directory, run `git status --short > "$SCRATCH/status-before.txt"`, `cp -r web "$SCRATCH/web.before"`, `cp -r test "$SCRATCH/test.before"`, `cp -r skills "$SCRATCH/skills.before"`, `cp -r codex "$SCRATCH/codex.before"`, `cp scripts/dashboard.mjs "$SCRATCH/dashboard.mjs.before"`, `cp src/skills/dashboard.md "$SCRATCH/dashboard.md.before"` and `npm test 2>&1 | tail -8 > "$SCRATCH/test-before.txt"`, and verify `diff -rq "$SCRATCH/web.before" web`, `diff -rq "$SCRATCH/test.before" test`, `diff -rq "$SCRATCH/skills.before" skills` and `diff -rq "$SCRATCH/codex.before" codex` each print nothing, and `$SCRATCH/test-before.txt` contains `pass 86` and `fail 0`
- [x] 1.2 Add the `mtimeOf(root, path)` helper beside `sniffBinary` in `scripts/dashboard.mjs` (design D7: `lstatSync(join(root, path)).mtimeMs` in a `try`, `null` in the `catch`, never `statSync` because a symbolic link must not be followed), give the tracked entry literal at `scripts/dashboard.mjs:163` an `mtimeMs: mtimeOf(root, path)` field, and rewrite the untracked loop's stat at `:186-190` so one `lstatSync` yields both `mtimeMs` and the `isFile()` test that guards `sniffBinary`, changing nothing else in the file, and verify `grep -n "statSync" scripts/dashboard.mjs` shows no new occurrence outside the pre-existing ones (compare with `grep -n "statSync" "$SCRATCH/dashboard.mjs.before"`), `diff -u "$SCRATCH/dashboard.mjs.before" scripts/dashboard.mjs` touches only `diffList` and the new helper, and a node one-liner that imports `diffList` and `detectGit` from `scripts/dashboard.mjs`, runs `diffList(process.cwd(), 'HEAD')` on this repository and prints `files.every((f) => 'mtimeMs' in f)` plus `files.filter((f) => typeof f.mtimeMs === 'number').length` reports `true` and a count greater than zero
- [x] 1.3 Add the `mtimeMs` assertions to `test/diff.test.mjs` per design D7 (import `lstatSync` from `node:fs`; extend the list test at `:53-73` with a numeric time for `keep.md`, `null` for the deleted `gone.md` and a numeric time for the untracked `fresh.md`; add the new test `diff: mtimeMs is the file's own lstat time and moves when the file is rewritten` that compares the API value with `lstatSync` exactly and again after rewriting `keep.md`, asserting `>=` rather than `>`; extend the symlink test at `:123-143` so `link.md`'s reported time equals the link's own `lstatSync` time), and verify `node --test test/diff.test.mjs` passes with one more test than before and the new test's name appears with a tick in the output

## 2. The pure navigation helpers on the client

- [x] 2.1 Add `flattenTree`, `ancestorDirs` and `diffNav` as exported, DOM-free functions after `buildTree` in `web/app.mjs` exactly as design D8 specifies (`flattenTree` recurses directories before files to match the render order at `web/app.mjs:404,407`; `diffNav` wraps at both ends, returns the first and last path when the current path is absent, and picks `latest` by strictly greater numeric `mtimeMs` so ties keep list order), touching no existing export, and verify `node -e "import('./web/app.mjs').then(m => console.log(typeof m.flattenTree, typeof m.ancestorDirs, typeof m.diffNav))"` prints `function function function` and `diff -u "$SCRATCH/web.before/app.mjs" web/app.mjs` shows the three functions as one added block with no other change in the pure-helper section
- [x] 2.2 Add the test `client: diff keyboard navigation helpers` to `test/diff.test.mjs` covering all twelve cases listed in design D8 (empty tree; the hand-written expected order `test/x/deep.mjs, web/app.css, web/app.mjs, README.md, a.md`; empty list; absent current path; unknown current path; a middle entry; both wrap-around ends; a one-entry list; `latest` with a `null`-time entry present; every time `null`; a tie on the greatest time; and the three `ancestorDirs` cases), and verify `node --test test/diff.test.mjs` reports the new test passing and that deliberately breaking one assertion locally makes it fail, so the test is known to execute rather than to be skipped

## 3. Stylesheet fixes in web/app.css

- [x] 3.1 Fix the progress bar by adding `display: block` to the existing `.bar .fill` rule at `web/app.css:160` and changing nothing else about it (design D1), and verify `grep -n "\.bar \.fill" web/app.css` shows one rule containing `display: block`, `height: 100%` and `transition: width`, and that with the dashboard running on this repository a headless-Chrome evaluation on `#/changes` reports `getComputedStyle(document.querySelector('.bar .fill')).display === 'block'` and a fill `getBoundingClientRect().width` strictly between zero and the parent `.bar` width for a change whose tasks are partly ticked
- [x] 3.2 Add `.content.wide { max-width: none; }` immediately after the `.content` rule at `web/app.css:97` (design D2), leaving the `.content` rule itself byte-identical (the router half of design D2, which sets the class, lands in task 5.3), and verify `grep -n -A1 "^\.content {" web/app.css` shows the original rule followed by the new one and `grep -c "max-width: 1100px" web/app.css` still reports exactly one
- [x] 3.3 Make the diff file pane sticky with its own scroll area per design D3 by appending `position: sticky`, `top: 16px`, `max-height: calc(100vh - 32px)` and `overflow: auto` to the existing `.diff-left` rule at `web/app.css:205`, then adding a **new** `@media (max-width: 1000px)` block **after** that rule which resets the pane to `position: static`, `max-height: none`, `overflow: visible`, leaving the existing media block at `web/app.css:204` byte-identical because a reset placed inside it would sit earlier in the cascade at equal specificity and be overridden by the base rule, and verify `grep -n "diff-left\|@media (max-width: 1000px)" web/app.css` shows the reset's media block on a line number greater than the base `.diff-left` rule's, and a headless-Chrome evaluation at a 1600 px viewport reports `getComputedStyle(el).position === 'sticky'` for `.diff-left` and `el.scrollHeight > el.clientHeight` on this repository's diff list, while at a 900 px viewport the same element reports `position === 'static'` and `max-height` of `none`
- [x] 3.4 Extend the scrollbar block so `.diff-left` and `.patch` join the inner-area group (design D4): add both selectors to the `::-webkit-scrollbar`, `-track` and `-thumb` lists at `web/app.css:241-246`, add `height: 16px` to that group's `::-webkit-scrollbar` rule, add a `::-webkit-scrollbar-corner` rule for both beside the textarea's at `:247`, and add both names to the `body, .editor textarea, .sidebar` line inside `@supports not selector(::-webkit-scrollbar)` at `:253`, and verify `grep -c -- "::-webkit-scrollbar" web/app.css` reports a higher count than `grep -c -- "::-webkit-scrollbar" "$SCRATCH/web.before/app.css"`, `grep -n "@supports not selector" web/app.css` still prints exactly one line whose block names `.patch` and `.diff-left`, and a headless-Chrome evaluation reports a non-zero `offsetHeight - clientHeight` on `.patch` for a patch with lines wider than the pane, meaning a horizontal bar is present to be styled
- [x] 3.5 Rewrite the diff row rules at `web/app.css:212-216` per design D11 (row and anchor both `align-items: center`; `padding: 2px 4px`; the anchor flexible with `min-width: 0` and `overflow-wrap: anywhere`; `li.file a:hover` neutralised to `background: none; color: inherit`; one selector list painting both `li.file:hover` and `li.file.selected`; `.tag.st` inverted to `color: var(--lime)` on `background: var(--lime-ink)` with a matching `border-color`; `.tag.latest` inverted to `color: var(--ink)` on `background: var(--paper)`), and verify `grep -n "li.file" web/app.css` shows `align-items: center` and no remaining `align-items: baseline` in the diff block, and a headless-Chrome evaluation on a selected row reports the computed `background-color` of `.tag.st` equal to the computed value of `--lime-ink` and its `color` equal to `--lime` in both the light and the dark emulated scheme
- [x] 3.6 Add the theme-control rules for the dropdown per design D5 (`.theme` as a column, `.theme-label`, `.select-wrap` with the `::after` `\25BE` indicator marked `pointer-events: none`, `.select-wrap:hover::after { color: var(--lime-ink); }` so the glyph does not stay near-white on the lime hover background in dark mode, and `.theme select` with `appearance: none`, `border-radius: 0`, the 2 px border, the 2 px hard shadow, paper background, ink colour and right padding for the glyph, plus hover and `:focus-visible`), replacing the three `.theme button` rules at `web/app.css:197-200`, and verify `grep -n "theme\|select-wrap" web/app.css` shows no rule still targeting `.theme button` or `aria-pressed` and does show both the `::after` and the `:hover::after` rules, `grep -n "url(\|@import\|http" web/app.css` prints nothing, and a node one-liner asserting the file's `{` and `}` counts are equal exits 0

## 4. The theme dropdown: markup and wiring

- [x] 4.1 Replace the three-button group at `web/index.html:25-29` with the label, `.select-wrap` and `<select id="theme-select">` carrying the three options `system`, `light` and `dark` exactly as design D5 writes them, leaving the pre-paint script at `web/index.html:8` and every other line of the file byte-identical, and verify `diff -u "$SCRATCH/web.before/index.html" web/index.html` shows a change only inside the `.theme` block, `grep -c "data-theme-choice" web/index.html` reports zero, and `grep -n "localStorage.getItem('my-flow.theme')" web/index.html` still prints line 8
- [x] 4.2 Rewire the client per design D6: inside `applyTheme` replace the `[data-theme-choice]` `aria-pressed` loop with setting `theme-select`'s `value`, and replace the three click listeners at `web/app.mjs:164-171` with one `change` listener that normalises the value, applies it, then removes or writes `THEME_KEY`, leaving `THEME_KEY`, `normalizeTheme`, `themeAttr` and the `storage` wrapper byte-identical, and verify `grep -c "data-theme-choice" web/app.mjs` reports zero, `diff -u "$SCRATCH/web.before/app.mjs" web/app.mjs` leaves lines 106 to 153 unchanged, and `node --test test/diff.test.mjs` still passes the `client: theme helpers` test
- [x] 4.3 Confirm the dropdown behaves as the spec requires by driving headless Chrome with `Emulation.setEmulatedMedia` (design, Rebuild section): under an emulated dark scheme choose `light`, under light choose `dark`, reload, then choose `system`, and repeat the `dark` choice with `localStorage.setItem` stubbed to throw, and verify the page reports `data-theme` equal to the chosen value in each case, the body background is the light palette under emulated dark and the dark palette under emulated light, the value survives the reload with the select showing the stored option, `system` removes the attribute and clears the key, the throwing-storage pass still switches the page for the current session, and under the dark scheme the computed `color` of the `.select-wrap::after` indicator equals the computed `--lime-ink` value while the control is hovered rather than staying near-white on lime

## 5. The diff page: marker, shortcuts and legend

- [x] 5.1 Give `pageDiff` a second parameter `out` and have it report the rendered order through that object rather than writing `state` itself, wiring the call site in the same task so the page still renders: declare `const out = {};` in `render()` immediately before the `job` IIFE at `web/app.mjs:453` and change the diff branch at `:460` from `return pageDiff(route.path);` to `return pageDiff(route.path, out);` (design D9), then set `out.order = []` in both early returns, the no-git branch at `web/app.mjs:395` and the failed-fetch branch at `:397`, and otherwise build the order once with `buildTree` followed by `flattenTree` for the tree view or `body.files` for the flat view, assign `out.order = ordered`, and compute `const latest = diffNav(ordered, null).latest` **before** `left` is assembled at `:407` because `fileRow` at `:399` closes over `latest`, then emit the marker in `fileRow` as `<span class="tag latest" title="most recently modified, ...">` carrying the escaped `when(f.mtimeMs)` value for that one entry only (design D10), and verify `grep -n "pageDiff(route.path, out)\|const out = {}" web/app.mjs` prints both lines with the declaration on a lower line number than the call, `grep -n "state.diffOrder" web/app.mjs` shows no assignment inside `pageDiff`, a headless-Chrome evaluation on this repository's diff page reports exactly one `.tag.latest` element in the tree view and exactly one in the flat view both on the same file path, touching a different changed file and pressing Refresh moves the marker to that file, and opening `#/diff` on a non-git project leaves the page's reported order empty
- [x] 5.2 Add the `.diff-tree .tag.latest` base rule to `web/app.css` after `.diff-tree .tag.st` (design D10: `min-width: 1.6em`, centred, `background: var(--ink)`, `color: var(--paper)`, `border-color: var(--border)`), and verify a headless-Chrome evaluation reports the marker's computed `background-color` equal to the computed `--ink` value and its `color` equal to `--paper` under both emulated schemes, and that on the selected row the D11 override applies instead
- [x] 5.3 Build the `keydown` handler in `wireDiff` and rewrite `render`'s commit block exactly as design D9 and D2 specify: in `wireDiff`, one listener on the document that returns early on `ctrlKey`, `altKey`, `metaKey`, `isComposing`, then on `state.route.page !== 'diff'` so the still-attached listener is inert during the render that follows navigating away, then on a target whose tag is `INPUT`, `SELECT` or `TEXTAREA` or which is content-editable, maps `j` to next, `k` to previous and `.` to latest through `diffNav(state.diffOrder ?? [], state.route.path)`, deletes the target's `ancestorDirs` from `state.diffClosed`, calls `preventDefault` only after a shortcut matched and navigates by setting `win.location.hash`, storing its remover in `state.unbindDiffKeys`; and in `render`, after the supersede check at `web/app.mjs:468` and before `view.innerHTML = html` at `:469`, call `state.unbindDiffKeys?.()`, clear it, commit `state.diffOrder = out.order ?? []`, and toggle the `wide` class with `view.classList.toggle('wide', route.page === 'diff')`, leaving the top of `render` at `:450-452` free of all four so no keystroke is dropped during the fetch and no superseded render disarms the winner, and verify `grep -n "unbindDiffKeys\|classList.toggle('wide'\|state.diffOrder =" web/app.mjs` shows every one of them below the `if (rendering !== job) return;` line and none above it, and a headless-Chrome run reports that `j` and `k` move the selection to the adjacent rows in rendered order and wrap at both ends, `.` selects the path the marker is on, focusing the theme `<select>` and pressing `j` leaves the selection unchanged, typing `j` inside the editor's textarea on a `#/file/...` route changes only the textarea's content, navigating away from and back to the diff page leaves exactly one handler bound so `j` advances by exactly one file, the `#view` element carries `wide` on `#/diff` and not on `#/changes`, pressing `j` twice in quick succession while a fetch is still in flight is not swallowed, and navigating from the diff page to Changes and pressing `j` before that render commits leaves the hash at `#/changes` rather than yanking it back to a `#/diff/...` path
- [x] 5.4 Add the shortcut legend to the diff page's `.row-actions` between the Refresh button and the file-count text, as three `.tag` key caps inside a `.meta` span (design D9), and add the selected-row `scrollIntoView({ block: 'nearest', inline: 'nearest' })` call beside the `wireDiff()` call in the same post-supersede block, and verify the rendered page shows `j next k previous . latest` next to the Tree / Flat list toggle in both themes, and that pressing `.` when the marked file is below the fold scrolls it into view inside `.diff-left` without moving the window's own scroll position
  - note: verified with the sticky pane pinned (window scroll unchanged, pane scrollTop 0 -> 138). At window scroll 0 the pane's bottom edge sits below the viewport, and `scrollIntoView` then also moves the window by that overflow (110 px at 1600x1000); recorded for mf-verify, design D9 kept as written.

## 6. Documentation and generated outputs

- [x] 6.1 Change the Theme bullet and extend the Diff bullet in `src/skills/dashboard.md` per design D12, then change line 217 and line 219 in all eight `README*.md` files so the theme sentence names a dropdown rather than buttons and the diff sentence mentions the latest-modified marker and the `j` / `k` / `.` shortcuts, keeping each translation in its own language, and verify `grep -rn "Dark buttons\|buttons Auto\|Schaltflächen Auto\|botones Auto\|boutons Auto\|Dark ボタン\|Dark 버튼\|Dark 按钮\|Dark 按鈕" README*.md src/skills/dashboard.md` prints nothing, which an unedited `README.md` would fail on its `the Auto / Light / Dark buttons` wording, so no button wording survives in any language, and ``grep -l "j` / `k`" README*.md`` lists all eight files, so every translation carries the untranslated key names in its diff sentence
- [x] 6.2 Regenerate the tool surfaces with `npm run build` and re-check with `npm run check`, and verify `npm run check` exits 0 reporting the outputs up to date, `diff -rq "$SCRATCH/skills.before" skills` and `diff -rq "$SCRATCH/codex.before" codex` each name only `skills/dashboard/SKILL.md` and `codex/skills/my-flow-dashboard/SKILL.md` as differing, and both regenerated files contain the new dropdown and shortcut wording

## 7. End-to-end evidence in both themes

- [x] 7.1 Drive headless Chrome over the DevTools protocol against the dashboard running on this repository, at a 1600 px wide viewport, forcing each theme with `Emulation.setEmulatedMedia` and `features: [{ name: 'prefers-color-scheme', value: 'light' }]` then `'dark'`, launching without `--hide-scrollbars` and capturing with `captureBeyondViewport` left false, covering success criteria 1 and 5: the diff page with a wide patch, a hovered row, the selected row and the marker, in both themes, writing the images under `.my-flow/verify/dashboard-ui-polish-shots/`, and verify at least eight images exist, each patch pane shows the flat square 16 px bar rather than a rounded grey default bar, the patch pane spans the window rather than stopping at 1100 px, and in the dark images the status letter is legible on both the hovered and the selected row with the row text and tags centred in the highlighted box
- [x] 7.2 In the same run, capture the Changes page in both themes with a change whose tasks are partly ticked, measure the fill and the bar with `getBoundingClientRect`, then tick one task in that change's `tasks.md` from outside the browser and measure again without reloading, covering success criterion 3, and verify the measured ratio matches `done/total` within one pixel before and after, the second measurement is larger than the first, no reload happened (the page's `performance.navigation` entry count is unchanged), and the Changes page's content still stops at 1100 px while the diff page does not
- [x] 7.3 In the same run, exercise the dropdown and the shortcuts end to end, covering success criteria 2 and 4, and verify the sidebar shows one dropdown listing Auto (system), Light and Dark, the four theme cases of task 4.3 hold, `/api/diff` entries carry a numeric `mtimeMs` for existing files and `null` for a deleted one, `j` / `k` / `.` move the selection as task 5.3 describes, and a keystroke typed into the editor's textarea never moves the diff selection
- [x] 7.4 Write the evidence note at `.my-flow/verify/dashboard-ui-polish-e2e-<UTC time>.md` in the shape of `.my-flow/verify/dashboard-theme-lock-diff-e2e-2026-09-09T16-46.md`, listing every check with PASS or FAIL, every screenshot with what it shows, and the measured numbers from task 7.2, and verify the note exists, names each of the proposal's success criteria 1 to 5 at least once, references every image written under `.my-flow/verify/dashboard-ui-polish-shots/`, and contains no FAIL line

## 8. Final gate

- [x] 8.1 Targeted verification of the whole change in one uninterrupted pass: run `npm test`, `npm run build`, `npm run check`, `grep -rE "https?://" web/` and a fresh light-mode and dark-mode screenshot of the diff page after any fix made since task 7.1, and verify `npm test` reports more than 86 passing and 0 failing, `npm run check` exits 0, the grep prints nothing and exits 1, and both fresh screenshots still show the styled scrollbar, the full-width patch and the readable status letter
- [x] 8.2 Clean up this change's own diff only, removing any leftover debug rule, commented-out declaration, stray `console.log` or duplicated selector introduced by groups 1 to 6, then re-verify by re-running `npm test` and re-reading each changed file's diff against the task 1.1 snapshot, and verify `diff -u` against the snapshot for `web/app.css`, `web/app.mjs`, `web/index.html` and `scripts/dashboard.mjs` contains no commented-out or duplicated declaration and no debugging statement, and `npm test` is still green
- [x] 8.3 Confirm the change touched nothing outside its declared Impact by comparing against the task 1.1 snapshot, remembering that `git status --short` collapses each untracked directory to a single line (`?? web/`, `?? changes/`, `?? specs/`, `?? skills/`) and so can never name the individual files this change edited, and verify `diff -rq "$SCRATCH/web.before" web` names only `app.css`, `app.mjs` and `index.html`, `diff -rq "$SCRATCH/test.before" test` names only `diff.test.mjs`, `diff -u "$SCRATCH/dashboard.mjs.before" scripts/dashboard.mjs` touches only `diffList` and the new `mtimeOf` helper, the ` M` lines of `git status --short` are exactly the eight `README*.md` files plus the same tracked files that were already modified in `$SCRATCH/status-before.txt`, the `??` lines are exactly the set already present in that snapshot with no new entry, and no file named in the design's Do-Not-Touch section appears in any of these comparisons
- [x] 8.4 Obtain an independent review in a fresh context with `/my-flow:mf-verify dashboard-ui-polish`, and verify the report written under `.my-flow/verify/` contains the line `Verdict: PASS`
- [x] 8.5 Declare the change done by running `node scripts/spec.mjs stage dashboard-ui-polish done`, and verify the command prints `dashboard-ui-polish: stage done` and `node scripts/spec.mjs status dashboard-ui-polish` shows every task ticked
