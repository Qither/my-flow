## 1. Fixture and library

- [x] 1.1 Create the fixture plugin `test/fixtures/plugin-demo/` exactly as design D11 lists it (`my-flow-plugin.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `skills/demo/SKILL.md`, `agents/demo-reviewer.md`, `hooks/hooks.json`, `hooks/demo-hook.mjs`, `scripts/hello.mjs`, `server/mcp.mjs` as a plain-Node newline-delimited JSON-RPC stdio server that answers `initialize`, `tools/list`, `tools/call` for `echo` and `ping`, accepts `notifications/initialized`, and logs to stderr only) and verify `node -e "for (const f of ['my-flow-plugin.json','.claude-plugin/plugin.json','.claude-plugin/marketplace.json','hooks/hooks.json']) JSON.parse(require('fs').readFileSync('test/fixtures/plugin-demo/'+f,'utf8'))"` exits 0, neither `test/fixtures/plugin-demo/package.json` nor `test/fixtures/plugin-demo/node_modules/` exists, and piping `{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18"}}` then `{"jsonrpc":"2.0","method":"notifications/initialized"}` then `{"jsonrpc":"2.0","id":2,"method":"tools/list"}` then `{"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"echo","arguments":{"text":"hi"}}}` (one per line) into `node test/fixtures/plugin-demo/server/mcp.mjs` prints exactly three stdout lines, the first containing `"protocolVersion":"2025-06-18"`, the second `"name":"echo"` and the third `"text":"hi"`, with nothing else on stdout
- [x] 1.2 Create `scripts/lib/plugins.mjs` (design D1, D2, D3) exporting `CORE_COMMANDS`, `registryPath()`, `readRegistry()`, `writeRegistry()`, `loadManifest(root)`, `substitutePluginRoot(value, root)`, `resolvePluginVerb(verb)` and the renderers `codexSkillText`, `codexAgentToml`, `codexHookCommand`, `codexMcpTables`, `claudeCommands`, using only `node:` modules and the exact error texts of the D2 table, and verify `node -e "import('./scripts/lib/plugins.mjs').then(async m => { const r = m.loadManifest('test/fixtures/plugin-demo'); console.log(r.errors.length, r.manifest.name); })"` prints `0 plugin-demo`, and `node -e "import('./scripts/lib/plugins.mjs').then(m => console.log(m.loadManifest('test').errors[0]))"` prints `missing my-flow-plugin.json`
- [x] 1.3 Create `test/plugin.test.mjs` with the unit tests of the design Test Plan for 1.1 and 1.2 (fixture manifest valid; the mutated copies each yield their D2 error text, including `codexSkillPrefix "my-flow-" is reserved for the core` and, for prefix `my-` with skill `flow-spec`, `skill "my-flow-spec" collides with the core skill directory "my-flow-spec"`, while prefix `my-flow-browser-` is accepted; `${PLUGIN_ROOT}` substitution; skill rewrite; agent TOML text; `[mcp_servers.plugin-demo]` text; the MCP server answering `initialize`, `tools/list`, `tools/call` and `ping`), every test using `makeTmp` and `cleanEnv` with temporary `MY_FLOW_HOME`, `CODEX_HOME`, `CLAUDE_CONFIG_DIR`, with a header comment stating that `package.json` lists test files explicitly so a bare `node --test` must never be used because it would pick up `test/fixtures/**`, and append `test/plugin.test.mjs` to the `test` script in `package.json`, and verify `node --test test/plugin.test.mjs` exits 0, `npm test` exits 0, and `grep -c "test/plugin.test.mjs" package.json` prints `1`

## 2. Plugin verbs and dispatch

- [x] 2.1 Create `scripts/plugin.mjs` with `add <path|git-url> [--name <n>] [--dry-run]`, `remove <name>`, `enable <name>`, `disable <name>` (design D3, D4: validation, atomic registry write, git clone into `<MY_FLOW_HOME>/plugins/`, `cloned` flag, printed `setup` line and the two install commands, exit codes 0 / 1 / 2), add `plugin: 'plugin.mjs'` and its usage line to `scripts/cli.mjs`, and verify that with `MY_FLOW_HOME` set to a fresh temp directory `node scripts/cli.mjs plugin add test/fixtures/plugin-demo` exits 0 printing `added plugin-demo 0.1.0` and `setup: pending`, `<MY_FLOW_HOME>/plugins.json` contains `"enabled": true` and `"cloned": false`, a second identical `plugin add` exits 1 with `already registered`, `plugin disable plugin-demo` then `plugin enable plugin-demo` then `plugin remove plugin-demo` each exit 0, and `plugin add` of a temp copy whose `contributes.cli` verb is `spec` exits 1 printing `cli verb "spec" collides with core command "spec"`
- [x] 2.2 Implement `plugin list [--json]` in `scripts/plugin.mjs` with the D5 text and JSON layouts, the setup probe, the Claude probe through `spawnCli('claude', ['mcp','get',<server>])` with a 10 000 ms timeout and the `MY_FLOW_PLUGIN_FAKE_CLAUDE` seam, and the Codex file probe, and verify that with the fixture registered in a temp `MY_FLOW_HOME`, a temp `CODEX_HOME`, a temp `CLAUDE_CONFIG_DIR` and `MY_FLOW_PLUGIN_FAKE_CLAUDE=missing`, `node scripts/cli.mjs plugin list` prints lines containing `plugin-demo 0.1.0 [enabled]`, `setup: pending`, `claude: unknown (claude not found)` and `codex: not installed`, `plugin list --json` parses with `plugins[0].setup.state === "pending"` and `plugins[0].mcpServers["plugin-demo"].args[0]` ending in `/test/fixtures/plugin-demo/server/mcp.mjs`, and with `MY_FLOW_HOME` pointing at an empty temp directory `plugin list` prints `no plugins registered`
- [x] 2.3 Add contributed-verb dispatch to `scripts/cli.mjs` (design D1, D6: core table first, the library loaded with a dynamic `await import('./lib/plugins.mjs')` only when `cmd` is not a core command or the usage text is printed, `resolvePluginVerb`, spawn with `MY_FLOW_PLUGIN_ROOT`, disabled-plugin note, missing-script error, usage lines for enabled plugin verbs) and verify that with the fixture registered `node scripts/cli.mjs demo-hello a b` exits 0 printing `hello from plugin-demo at <abs fixture path>` and `a b`, `node scripts/cli.mjs nope` exits 1 printing the usage text, after `plugin disable plugin-demo` the same `demo-hello` exits 1 with stderr containing `which is disabled`, `grep -c "^import.*lib/plugins.mjs" scripts/cli.mjs` prints `0`, and with `<MY_FLOW_HOME>/plugins.json` containing only `{` `node scripts/cli.mjs build --check` exits 0 with the same output as with no registry file
- [x] 2.4 Add the registry and dispatch tests to `test/plugin.test.mjs` (round trip `add` -> `list --json` -> `disable` -> `enable` -> `remove`; a second temp home does not list the plugin; duplicate add refused; `add --dry-run` leaves no `plugins.json`; core-collision refusal; setup never executed: `add` of a mutated copy whose `setup` is `node <temp>/sentinel.mjs` (a script writing `<temp>/sentinel.txt`) prints the setup line and `<temp>/sentinel.txt` does not exist afterwards; corrupt `plugins.json` makes `build --check` behave as with no registry; `list` states `setup: pending`, `setup: ok` on a temp copy with an empty `node_modules/`, `claude: unknown (claude not found)`, `claude: installed` with `MY_FLOW_PLUGIN_FAKE_CLAUDE=plugin-demo` and a planted `<CLAUDE_CONFIG_DIR>/plugins/installed_plugins.json`; verb dispatch, unknown verb exit 1, disabled note; git clone through `add file://<temp repo>` and clone deletion on `remove`, skipped when `hasGit()` is false) and verify `npm test` exits 0 and `node --test test/plugin.test.mjs` reports at least 12 passing tests and 0 failing

## 3. Install and uninstall surfaces

- [x] 3.1 Add the `schtasks(args)` helper to `scripts/install.mjs` (design D10: whenever `MY_FLOW_SCHTASKS` is defined spawn `process.execPath <value> <args>`, otherwise `schtasks <args>`, exactly as `scripts/lib/models.mjs:130-133` does) at the three spawn sites in `registerLauncher` and `unregisterSurface`, and document the seam in the file header, and verify that, with CODEX_HOME and MY_FLOW_HOME set to the same two empty temp directories for both runs, `node scripts/install.mjs codex --dry-run` captured before and after the edit produces an empty diff (with the real homes the `backup <file> -> .../<timestamp>` lines differ by the second, and with different temp homes the `write .../config.json` and `--home` lines differ) and, with `MY_FLOW_SCHTASKS` pointing at a temp `.mjs` that writes its argv to a file and exits 0 and with temp `MY_FLOW_HOME` / `CODEX_HOME`, a real `node scripts/install.mjs codex` exits 0 and the argv file contains `/create`, and a following `node scripts/install.mjs --uninstall codex` exits 0 and the argv file contains `/delete`
- [x] 3.2 Implement the plugin skill and agent rendering of `install codex` (design D7 failure path and steps 1-3: registry read once with an unreadable registry treated as empty plus a stderr warning, `skip plugin <name>: <first error>` for a missing path or broken manifest without aborting the core install, removal of stale marker-bearing skill directories and plugin-header agent TOMLs, copy with `name:` rewrite, `${CLAUDE_PLUGIN_ROOT}` and `/plugin-demo:demo` rewrites, `.my-flow-plugin` marker, `replace` for a marker-bearing target under `--dry-run` and `skip` only for an unmanaged one, agent TOML with the `# my-flow agent: <role> (plugin <name>, ...)` header, always-copy note under `--link`) and verify that with the fixture registered and temp homes `node scripts/install.mjs codex --dry-run` prints `add <CODEX_HOME>/skills/plugin-demo-demo` and `add <CODEX_HOME>/agents/demo-reviewer.toml` and creates nothing under `CODEX_HOME`, and a real `install codex` (with the `MY_FLOW_SCHTASKS` fake) produces `skills/plugin-demo-demo/SKILL.md` whose text contains `name: plugin-demo-demo` and `$plugin-demo-demo` and no `${CLAUDE_PLUGIN_ROOT}`, `skills/plugin-demo-demo/.my-flow-plugin` containing `plugin-demo`, and `agents/demo-reviewer.toml` whose first line starts with `# my-flow agent: demo-reviewer (plugin plugin-demo`, a second `install codex --dry-run` then prints `replace <CODEX_HOME>/skills/plugin-demo-demo` and no `skip` line for it, and, in a second empty MY_FLOW_HOME (the first home already holds plugin-demo, and a copy is refused there by the D2/D3 collision rules even with --name), after `plugin add <temp copy>` and deleting that copy's directory, `install codex` with the MY_FLOW_SCHTASKS fake exits 0 printing `skip plugin plugin-demo: path <forward-slash absolute copy path> is missing` while `<CODEX_HOME>/AGENTS.md` is still written
- [x] 3.3 Implement plugin hooks and MCP tables in `install codex` (design D7 steps 4-6: hook groups appended after the core groups through the shim form, trust hashes from the existing loop, `[mcp_servers.<server>]` tables inside the managed block with `JSON.stringify` strings, `startup_timeout_sec`, optional env table, the duplicate scan of the unmanaged text and the exact warning `skip [mcp_servers.<server>]: defined outside the my-flow block; remove it first to let my-flow manage it`) and verify `node scripts/install.mjs codex --dry-run` with the fixture registered prints `[dry-run] add hook SessionStart: powershell.exe -NoProfile -ExecutionPolicy Bypass -File "<shim>" -Script "<abs fixture>/hooks/demo-hook.mjs"`, `[dry-run] add [mcp_servers.plugin-demo]`, `[dry-run]   command = "node"` and `[dry-run]   startup_timeout_sec = 20` and creates nothing under CODEX_HOME, and that a real `install codex` into temp homes yields a `hooks.json` whose parsed `hooks.SessionStart` has the plugin group as its last entry and that entry's command string ends with `-Script "<abs fixture, forward slashes>/hooks/demo-hook.mjs"` (the raw file JSON-escapes the quotes as \", so the check parses the file rather than grepping it), a `config.toml` whose managed block contains `[mcp_servers.plugin-demo]`, `command = "node"`, `startup_timeout_sec = 20` and a `[hooks.state.` entry for the plugin hook, and that with `[mcp_servers.plugin-demo]\ncommand = "x"\n` planted above the block beforehand `install codex --dry-run` prints the exact warning line and a real install leaves exactly one `[mcp_servers.plugin-demo]` in the file (`grep -c "^\[mcp_servers.plugin-demo\]" <CODEX_HOME>/config.toml` prints `1`)
- [x] 3.4 Extend `uninstall codex` (design D8: remove marker-bearing skill directories and plugin-header agent TOMLs in addition to the existing marker steps) and verify that after planting `<CODEX_HOME>/skills/plugin-demo-keep/SKILL.md` (no marker), a user `[mcp_servers.other]` table and a user hook entry in `hooks.json` (all planted with LF endings and exactly one trailing newline, hooks.json written as JSON.stringify(doc, null, 2) + '\n' and config.toml without a run of three newlines, because install and uninstall re-serialise hooks.json with two-space indent and stripTomlBlock collapses blank runs and trims the tail, install.mjs:252,281), a real `install codex` followed by `node scripts/install.mjs --uninstall codex` leaves `config.toml`, `hooks.json` and `skills/plugin-demo-keep/` byte-identical to their planted content, and `skills/plugin-demo-demo/` and `agents/demo-reviewer.toml` no longer exist
- [x] 3.5 Extend `install claude` and `uninstall claude` (design D9: per enabled plugin the three printed commands, per server one `claude mcp add --transport stdio --scope user <server> -- node "<abs>/server/mcp.mjs"` line, `claude mcp remove` and `claude plugin disable` on uninstall, nothing executed) and verify that with the fixture registered and temp homes `node scripts/install.mjs claude --dry-run` prints `claude plugin marketplace add "<abs fixture>"`, `claude plugin install plugin-demo@plugin-demo` and `claude mcp add --transport stdio --scope user plugin-demo -- node "<abs fixture>/server/mcp.mjs"`, `CLAUDE_CONFIG_DIR` contains no new file, `node scripts/install.mjs --uninstall claude --dry-run` prints `claude mcp remove plugin-demo`, and with a temp directory holding a fake `claude.cmd` (plus a `claude` shell script on non-Windows) that writes `<temp>/claude-ran.txt` prepended to `PATH`, neither `install claude --dry-run` nor a real `install claude` into the temp `CLAUDE_CONFIG_DIR` (with the `MY_FLOW_SCHTASKS` fake) creates `<temp>/claude-ran.txt`
- [x] 3.6 Add the integration tests of the design Test Plan for 3.1-3.5 to `test/plugin.test.mjs` (codex dry-run prints the skill add line, the agent target, the hook add line and the [mcp_servers.plugin-demo] table lines and writes nothing under CODEX_HOME; uninstall codex --dry-run after a real install prints `[dry-run] remove <join(CODEX_HOME,'skills','plugin-demo-demo')>` and leaves the directory in place; no-plugin parity: with an empty MY_FLOW_HOME, `install codex --dry-run` output contains no `plugin`, `add hook` or `[mcp_servers.` line and equals a capture taken with the same empty homes, proving the core output stays unchanged; duplicate skip warning and single table; real install / uninstall byte-identical planted files and no marker left; disabled plugin removed on the next install; second dry-run prints `replace` not `skip` for the plugin skill; registered plugin with a deleted directory yields exit 0 and the `skip plugin plugin-demo: path <abs> is missing` line; corrupt `plugins.json` yields exit 0 and a stderr warning for `install codex --dry-run`; Claude commands printed with the absolute path; fake `claude.cmd` sentinel on `PATH` absent after dry-run and real `install claude`; `node scripts/cli.mjs build --check` spawned with process.execPath exits 0 printing `generated files are up to date` with the fixture registered in a temp MY_FLOW_HOME (never spawnSync('npm') from a test: it fails with EINVAL on Windows without a shell)) and verify `npm test` exits 0, `npm run check` exits 0, and `node --test test/plugin.test.mjs` reports 0 failing tests

## 4. Spike and documentation

- [x] 4.1 Run the D2 spike without executing any `claude plugin` or `claude mcp` command yourself: write `.my-flow/tmp/plugin-contract-spike.md` with the commands the user runs outside the session (`claude plugin marketplace add "<abs>/test/fixtures/plugin-demo"`, `claude plugin install plugin-demo@plugin-demo`, then compare the `plugin-demo` entry in `~/.claude/plugins/installed_plugins.json` with the fixture path, then `claude plugin uninstall plugin-demo@plugin-demo` and `claude plugin marketplace remove plugin-demo`), ask the user for the observed path, and record under `### Spike result (D2)` in `changes/plugin-contract/design.md` exactly one line reading `spike result: copied to ~/.claude/plugins/<...>` or `spike result: used in place (<path>)` or, when the user has not run the commands, `spike pending: user has not run the commands`, continuing with 4.2 regardless, and verify `grep -c "^spike result: \|^spike pending: user has not run the commands" changes/plugin-contract/design.md` prints `1`
- [x] 4.2 Add `## 8. Plugins` to `src/core/core.md` after `## 7. Paths` (design D12: the manifest, the verbs, the install flow, the `my-flow <verb>` rule, about eight lines, no `{{CALL}}` needed) and run `npm run build`, and verify `npm run check` exits 0, `grep -c "## 8. Plugins" claude/CLAUDE.block.md codex/AGENTS.block.md` prints `1` for both files, and `git diff --stat -- scripts/build.mjs manifest.json skills agents codex/skills codex/agents` is empty
- [x] 4.3 Add to each of `README.md`, `README.zh-CN.md`, `README.zh-TW.md`, `README.ja.md`, `README.ko.md`, `README.de.md`, `README.fr.md`, `README.es.md` a CLI-table row for `plugin add|remove|list|enable|disable [--json] [--dry-run]` and plugin verbs placed after the `models` row (line 143 in all eight files), a `## Plugins` section (translated heading allowed) directly before the intent-layer section describing `my-flow-plugin.json`, the registry, the verbs, `install codex` rendering with the duplicate-table warning, the printed Claude commands and the `my-flow <verb>` rule, plus its table-of-contents entry with renumbering, and verify `grep -l "my-flow-plugin.json" README*.md | wc -l` prints `8`, `grep -c "plugin add" README*.md` prints at least `2` for every file, and `npm run check` still exits 0

## 5. Final gate

- [x] 5.1 Targeted verification of the whole change (build, tests, Rebuild / Re-run steps)
- [x] 5.2 Cleanup of own diff only, then re-verify
- [x] 5.3 Independent verification report says PASS (.my-flow/verify/)
