## 1. mf-plan source skill

- [x] 1.1 In `src/skills/mf-plan.md` change `argument-hint` (`:4`) to `"<change-name | free text> [--deliberate] [--fast [--go]]"`, add a `## High-risk categories` section holding the one list (auth, migrations, destructive operations, public API changes, build config or shader pipeline changes, engine modules) with the sentence that it is judged from the proposal text and the paths it names, rewrite the `--deliberate` input line (`:21-23`) to reference that section instead of repeating the list, and add the two input lines for `--fast` (user-passed only, never model-selected, refused for the high-risk categories) and `--go` (only with `--fast`), and verify `grep -c "auth, migrations, destructive operations" src/skills/mf-plan.md` prints 1, `grep -n "## High-risk categories" src/skills/mf-plan.md` prints one line, and `grep -c -- "--fast\|--go" src/skills/mf-plan.md` is at least 3
- [x] 1.2 Add a `## Fast lane (--fast)` section to `src/skills/mf-plan.md` describing, in order: the guard (on a high-risk match print one line `--fast refused: <category>; planning in full mode.` and continue at `## Steps`), then the single-context authoring (read the files the proposal names; for free text write `proposal.md` inline as `## Inputs` already requires; write `design.md` with the required sections and `Plan mode: fast (one critic pass, no architect).` as the first line under `## Context`; write `tasks.md` in the strict format; no planner, no PLAN-DR header, no architect), exactly one read-only `critic` review, on `REJECT` one round of fixes and no second review with any unfixed finding listed in the handoff, then delta specs plus `{{CALL:spec}} validate <name>` and `{{CALL:spec}} stage <name> mf-plan` as in the full mode, and verify `grep -n "Plan mode: fast" src/skills/mf-plan.md` prints one line, `awk '/^## Fast lane/{f=1;next} /^## /{f=0} f' src/skills/mf-plan.md | grep -c "planner"` prints 0 (no planner delegation inside the fast section), `awk '/^## Fast lane/{f=1;next} /^## /{f=0} f' src/skills/mf-plan.md | grep -c "architect"` prints 1 (the "no architect" phrase only), and `grep -c "planner" src/skills/mf-plan.md` still prints 5 (the full-mode mentions at `:3`, `:33`, `:40`, `:50`, `:55` are untouched)
- [x] 1.3 Add a `## Continue into execute (--go)` section to `src/skills/mf-plan.md` per design D1: shared prose (valid only with `--fast` and only after the handoff printed; run `{{CALL:spec}} stage <name> execute`; print the execute goal statement once as a standing instruction without stopping; `--team` and `--worktree` are unavailable here, stop after the handoff and point at `{{CALL:execute}} <name>` instead; end with execute's Run report), then a `<!-- MY-FLOW:CLAUDE -->` block saying to invoke `my-flow:execute <name>` with the Skill tool and follow the loaded text with the section 2 goal stop skipped per D2 (Load step 4 is the stage call just made) and the Stop-hook execute-guard as the backstop, then a `<!-- MY-FLOW:CODEX -->` block saying to follow `{{CALL:execute}}` by reference (Load steps 1-3, task loop, final gate) with `get_goal` / `create_goal` running normally, add the two D7 one-line refusals (`--go` without `--fast`; `--fast` with `--deliberate`, where `--deliberate` wins and the refusal uses the guard's wording), and update the Handoff block (`:73-80`) with the fast `Review:` variants and the optional `Remaining findings:` line per design D6, and verify `grep -c "<!-- MY-FLOW:CLAUDE -->" src/skills/mf-plan.md` prints 2 and `grep -c "<!-- MY-FLOW:CODEX -->" src/skills/mf-plan.md` prints 2 (one pre-existing pair at `:49-57` plus the new pair, every block closed: `grep -c "MY-FLOW:" src/skills/mf-plan.md` prints 8), `awk '/MY-FLOW:CLAUDE/,/\/MY-FLOW:CLAUDE/' src/skills/mf-plan.md | grep -c "Skill tool"` prints 1 and the same awk over the CODEX ranges prints 0, `grep -n "Review: fast (critic" src/skills/mf-plan.md` prints one line, and `grep -c -- "--deliberate wins\|--go requires\|requires \`--fast\`" src/skills/mf-plan.md` is at least 2 (both D7 refusals stated)

## 2. execute source skill

- [x] 2.1 In `src/skills/execute.md` section 2, inside the `<!-- MY-FLOW:CLAUDE -->` block and directly after the existing "skip the goal" sentence (`:45`), add the single D2 clause: entering from `{{CALL:mf-plan}} --fast --go` in the same conversation counts as the user declining the goal, so do not print the block and do not stop, start task 1 at once, and the stage written in step 1.4 keeps the Stop-hook backstop armed, and verify `grep -n -- "--fast --go" src/skills/execute.md` prints exactly one line inside the CLAUDE block (`awk '/MY-FLOW:CLAUDE/,/\/MY-FLOW:CLAUDE/' src/skills/execute.md | grep -c -- "--fast --go"` prints 1), `awk '/MY-FLOW:CODEX/,/\/MY-FLOW:CODEX/' src/skills/execute.md | grep -c -- "--go"` prints 0, and `git diff --numstat src/skills/execute.md` shows at most 4 added and 1 removed line

## 3. Core block and READMEs

- [x] 3.1 In `src/core/core.md` insert the stage-table row `| Concrete, multi-file, design already clear | \`mf-plan --fast [--go] -> execute -> mf-verify\` |` after the "Concrete, multi-file or multi-module" row (`:29`) and add one Rules bullet saying `mf-plan --fast` is a fast lane the user asks for (one critic pass, no planner or architect), never selected by the model and never used for the high-risk row, and verify `grep -c "design already clear" src/core/core.md` prints 1 and, after task 4.1's build, the same string appears exactly once in `claude/CLAUDE.block.md` and once in `codex/AGENTS.block.md`
- [x] 3.2 In `README.md` add the same stage-table row after `:93` written with the arrow glyph that table already uses (`| Concrete, multi-file, design already clear | \`mf-plan --fast [--go] → execute → mf-verify\` |`), extend the `mf-plan` skill-table row (`:151`) to `mf-plan <name | text> [--deliberate] [--fast [--go]]` with a sentence in the "what it does" cell for the fast lane (you write the artifacts, one critic pass, no planner or architect, refused for the high-risk categories, `--go` continues into execute), note in the `execute` row (`:152`) that it can be entered from `mf-plan --fast --go`, add the `--go` exception to the "One loop authority per session" paragraph (`:117`), and extend the `/plan` vs `mf-plan` bullet (`:164`) with one sentence placing the fast lane between plan mode and the full consensus run, and verify `grep -c -- "--fast" README.md` is at least 4 and `grep -n "design already clear" README.md` prints one line inside the stage table (line number between 92 and 97)
- [x] 3.3 Mirror the four README edits of task 3.2 into `README.de.md`, `README.es.md`, `README.fr.md`, `README.ja.md`, `README.ko.md`, `README.zh-CN.md` and `README.zh-TW.md`, each in that file's own language and at the same table rows and paragraphs (stage table after the multi-file row, the `mf-plan` and `execute` skill rows, the loop-authority paragraph, the `/plan` vs `mf-plan` bullet), and verify `for f in README.md README.de.md README.es.md README.fr.md README.ja.md README.ko.md README.zh-CN.md README.zh-TW.md; do c=$(grep -c -- "--fast" "$f"); a=$(grep -n "| \`mf-plan → execute → mf-verify\` |" "$f" | head -1 | cut -d: -f1); b=$(grep -n -- "--fast \[--go\] → execute" "$f" | head -1 | cut -d: -f1); { [ "$c" -ge 4 ] && grep -q -- "--go" "$f" && [ "$b" = "$((a+1))" ]; } || echo "bad in $f (count $c, row $a/$b)"; done` prints nothing

## 4. Build, tests, installed surfaces

- [x] 4.1 Run `node scripts/build.mjs`, then `node scripts/build.mjs --check` and `npm test`, and verify both commands exit 0, `npm test` is green, `grep -c -- "--fast" skills/mf-plan/SKILL.md codex/skills/my-flow-mf-plan/SKILL.md` reports a non-zero count for both, `grep -c "/my-flow:execute" skills/mf-plan/SKILL.md` and `grep -c '\$my-flow-execute' codex/skills/my-flow-mf-plan/SKILL.md` are both at least 1, `grep -c "MY-FLOW:" skills/mf-plan/SKILL.md codex/skills/my-flow-mf-plan/SKILL.md` prints 0 for both, `grep -c "execute-guard" codex/skills/my-flow-mf-plan/SKILL.md` prints 0 (the Claude-only block did not leak), `grep -c -- "--fast --go" skills/execute/SKILL.md` is at least 1 while `grep -c -- "--fast --go" codex/skills/my-flow-execute/SKILL.md` prints 0 (the execute clause lives in the CLAUDE-only block), and `grep -n "argument-hint" skills/mf-plan/SKILL.md` shows the new hint
- [x] 4.2 Install the rendered surfaces so the real runs of group 5 use the new text: `node scripts/install.mjs codex --dry-run` first, then `node scripts/install.mjs codex` and `node scripts/install.mjs claude` (the latter upserts the working-agreement block into `~/.claude/CLAUDE.md`), and refresh the Claude plugin cache (`claude plugin update my-flow@my-flow`; if the plugin is not installed from a marketplace, record that the runs use `--plugin-dir` on this checkout instead), and verify `grep -c -- "--fast" "$HOME/.codex/skills/my-flow-mf-plan/SKILL.md"` is non-zero, `grep -c "design already clear" "$HOME/.claude/CLAUDE.md"` prints 1, and the installed Claude `skills/mf-plan/SKILL.md` under the plugin cache path reported by `node -e "const r=require(require('os').homedir()+'/.claude/plugins/installed_plugins.json').plugins['my-flow@my-flow'];console.log((Array.isArray(r)?r[0]:r).installPath)"` also contains `--fast`

## 5. Real runs on a throwaway probe change

- [x] 5.1 Fast happy path: `node scripts/spec.mjs new fast-lane-probe`, write a two-paragraph `proposal.md` for a concrete, low-risk change whose whole scope is one scratch file (create `.my-flow/probe/notes.txt` with two named lines), then run the fast lane on it exactly as the installed skill text prescribes (native `/my-flow:mf-plan --fast fast-lane-probe` in a session started after task 4.2, or by following the installed `SKILL.md` verbatim; record which form was used), and verify all five hold: exactly one subagent appeared and it was `critic` (no `planner`, no `architect` in the transcript), `grep -c "Plan mode: fast" changes/fast-lane-probe/design.md` prints 1, `grep -c "PLAN-DR" changes/fast-lane-probe/design.md` prints 0, `node scripts/spec.mjs validate fast-lane-probe` exits 0 with no errors, and the printed handoff line matches `Review: fast (critic OKAY|REJECT-fixed, no architect)`
- [x] 5.2 Guard refusal: run the fast lane on free text that names a database migration and stays otherwise tiny (for example "--fast: add a schema migration step to the probe notes file"), and verify the reply prints a one-line `--fast refused: ...` naming migrations, then continues in full mode (the `planner` subagent is spawned; recording that the planner appeared is enough evidence and the run may be interrupted afterwards), that a second run combining `--fast` with `--deliberate` prints the D7 refusal saying `--deliberate` wins and plans in full mode with the pre-mortem and test plan, and that neither refusal run wrote a fast marker anywhere new (`grep -rl "Plan mode: fast" changes/` lists only `changes/fast-lane-probe/design.md` from task 5.1; any additional change directory this run created is recorded here and deleted in task 5.4)
  - evidence: run 1 printed `--fast refused: migrations; planning in full mode.` and a planner subagent appeared (interrupted after its first reply); run 2 printed `--fast refused: --deliberate wins; planning in full mode.` and a planner appeared for the --deliberate full mode; no change directory was created (`ls changes/` = archive, dashboard-design-parity, fast-lane-probe, mf-plan-fast); `grep -rl "Plan mode: fast" changes/` lists `changes/fast-lane-probe/design.md` plus the four `changes/mf-plan-fast/` documents, which match only because they define the marker string; no other change carries it
- [x] 5.3 Hand-over: with `changes/fast-lane-probe/tasks.md` from task 5.1 still unticked, run the fast lane with `--fast --go` on the probe (re-planning is fine; the second planning pass must again be single-context) and let it enter the task loop, stopping the probe run as soon as its task 1.1 is ticked with evidence and before the probe's own final gate, and verify the transcript contains no `Paste this` and no `/goal ` line while the goal statement itself was printed once as a standing instruction, the execute skill text was loaded through the Skill tool rather than paraphrased, `node -e "console.log(require('./.my-flow/state/current-change.json').stage, require('./.my-flow/state/current-change.json').change)"` prints `execute fast-lane-probe`, task 1.1 of the probe is ticked with its verification evidence shown, `.my-flow/probe/notes.txt` exists with the two named lines, and a separate run of the fast lane with `--go` but without `--fast` prints the one-line refusal from design D7 and plans in full mode instead
  - evidence: second single-context pass, one critic (OKAY, no fixes); handoff printed with `Review: fast (critic OKAY, no architect)` and the line `Continuing into execute (--go): goal stop skipped, execute-guard armed.`; the goal statement was printed once as a standing instruction (no `Paste this`, no `/goal ` line in the transcript); `spec stage fast-lane-probe execute` run by the lane; the execute skill was loaded through the Skill tool (`Launching skill: my-flow:execute`) and followed, not paraphrased; note: the Skill tool served the session-start copy of execute (skills load at session start, design Risks), which predates the 4.1 clause, so the D2 rule was applied from the mf-plan text; state file printed `execute fast-lane-probe`; probe task 1.1 ticked with its byte-exact verify exiting 0 and `.my-flow/probe/notes.txt` holding the two lines; run stopped before the probe final gate; separate run with `--go` but no `--fast` printed `--go requires --fast; planning in full mode.` and a planner appeared
- [x] 5.4 Clean up the probe: delete `changes/fast-lane-probe/` and any other change directory created during 5.1-5.3, delete `.my-flow/probe/`, and run `node scripts/spec.mjs stage mf-plan-fast execute`, and verify `ls changes/` lists exactly `archive`, `dashboard-design-parity` and `mf-plan-fast`, `test ! -d .my-flow/probe && echo clean` prints `clean`, `node -e "console.log(require('./.my-flow/state/current-change.json').change)"` prints `mf-plan-fast`, `node scripts/spec.mjs validate` exits 0, and `git status --short | grep -c "fast-lane-probe"` prints 0 (note: `changes/` is untracked and the tree already carries the uncommitted `agent-model-routing` work, so `git status` cannot be used as a whole-tree cleanliness check)

## 6. Final gate

- [x] 6.1 Targeted verification of the whole change: `node scripts/build.mjs --check`, `npm test`, `node scripts/spec.mjs validate mf-plan-fast`, and a scan of the changed files for placeholder TODOs, `test.skip` / `.only` and half-written sentences, then cleanup of own diff only and a re-run of the same three commands, and verify all three exit 0 both times and `grep -n "TODO\|test\.skip\|\.only(" src/skills/mf-plan.md src/skills/execute.md src/core/core.md README.md README.de.md README.es.md README.fr.md README.ja.md README.ko.md README.zh-CN.md README.zh-TW.md` prints nothing (the eleven files this change edits; a `git diff --name-only` scan cannot pass here because the tree still carries the previous change's prose)
  - evidence: both passes: build --check exit 0, validate ok, npm test green (120 pass); the literal eleven-file grep prints only pre-existing prose (execute.md cleanup rule, core.md rule, the Stop-hook row of each README) and none of it is in this change; the scan restricted to the lines this change added (`git diff -U0 <eleven files> | grep ^+`) prints nothing; no cleanup was needed
- [x] 6.2 Independent review through `/my-flow:mf-verify mf-plan-fast` in a separate context (optionally `/my-flow:ask codex` on the diff first), and verify a report under `.my-flow/verify/` contains `Verdict: PASS`, then run `node scripts/spec.mjs stage mf-plan-fast done` and verify `node -e "console.log(require('./.my-flow/state/current-change.json').stage)"` prints `done`
