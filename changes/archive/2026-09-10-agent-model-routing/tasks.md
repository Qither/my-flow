## 1. Baseline: inherit (ships on its own)

- [x] 1.1 In `manifest.json` replace every `agents.<role>.claude` with `{ "model": "inherit" }`, drop `model` from every `agents.<role>.codex` (keep `model_reasoning_effort` high/high/medium/medium), and add `"sandbox_mode": "read-only"` to the codex block of architect and critic only (not planner, not verifier: user decision, see design Risks), and verify `node -e "const m=require('./manifest.json').agents;for(const c of Object.values(m)){if(c.claude.model!=='inherit'||c.codex.model)process.exit(1)};console.log(Object.entries(m).filter(([,c])=>c.codex.sandbox_mode==='read-only').map(([r])=>r).join(','))"` prints exactly `architect,critic`
- [x] 1.2 In `scripts/build.mjs` add the TOML line `c.sandbox_mode ? \`sandbox_mode = ${JSON.stringify(c.sandbox_mode)}\` : null` after the effort line (`:122`), run `node scripts/build.mjs`, and verify `node scripts/build.mjs --check` exits 0, `grep -c "^model: inherit$" agents/*.md` reports 1 for all four files, `grep -l "^model = " codex/agents/*.toml` prints nothing, `grep -l 'sandbox_mode = "read-only"' codex/agents/*.toml` lists exactly `codex/agents/architect.toml` and `codex/agents/critic.toml` and no other file, and `npm test` is green
- [x] 1.3 In `README.md` "Subagent roles" (`:169-174`) rename the `Claude model` column to `Model` with value `inherit` for all four rows, keep `Codex effort`, add a `Codex sandbox` column (none / read-only / read-only / none, with a note that the verifier stays prompt-enforced so it can run tests), and add one sentence under the table saying both CLIs inherit the main session's model and that `models` (section CLI commands) can override it locally, and verify `grep -c "| inherit |" README.md` prints 4 and `grep -c "opus\|sonnet" README.md` no longer matches inside the role table (`sed -n 167,177p README.md | grep -c "opus\|sonnet"` prints 0)

## 2. Override library and renderer

- [x] 2.1 Create `scripts/lib/models.mjs` with `myFlowHome()` (env `MY_FLOW_HOME` or `~/.my-flow`), `ROLES` read from the script root's `manifest.json`, `baseline()` returning the D1 constants (`claude.model = 'inherit'`, codex effort and no model), `readConfig()`, atomic `readState()` / `writeState()` (write `<file>.<pid>.tmp` then `renameSync`), `appendLog()` with rotation above 256 KB, and `readModels()` (null when missing or unparsable), and verify `MY_FLOW_HOME=<tmp> node -e "import('./scripts/lib/models.mjs').then(m=>{console.log(m.ROLES.join(','), JSON.stringify(m.baseline()), m.readModels()===null)})"` prints `planner,architect,critic,verifier`, a baseline with `inherit` for every role, and `true`
- [x] 2.2 Implement `readModelsCache()` (read-only, fail-open parse of `<CODEX_HOME>/models_cache.json`), `resolveAllowed()` (`claude` = settings.json model ∪ `knownModels`; `codex` = config.toml main model ∪ listed cache slugs ∪ `knownModels`; `efforts(slug)` from the cache with fallback `low|medium|high|xhigh`) and `validateRecommendation(obj, allowed)` per design D5 (exact role key set, claude alias regex or allowed set, codex `null` or allowed set, effort in `efforts(codex.model ?? main model)` or `null`, reason trimmed to 2000 chars), and start `test/models.test.mjs` with table-driven cases (valid baseline, valid full map, missing role, extra role, `gpt-7`, `claude-unknown`, a hidden slug, non-object; effort `ultra` rejects with a fixture cache listing `gpt-5.5` without `ultra`, passes with `gpt-6-astra` listed with `ultra`, rejects with no cache file while `xhigh` passes), and verify `node --test test/models.test.mjs` passes those cases with `CODEX_HOME` pointed at the fixture
- [x] 2.3 Implement `renderClaudeAgent`, `renderCodexAgent`, `locateClaudeSurface` (installed_plugins.json accepting an array or a single object under `plugins["my-flow@my-flow"]`, ignoring entries without a string `installPath`, treating a parse error as "no registry" logged once and falling back to the recorded plugin root; every path restricted to `<CLAUDE_HOME>/plugins/`), `locateCodexSurface` (`config.codex.agentsDir`, else `<CODEX_HOME>/agents`, files must start with `# my-flow agent`), `applyRoles(roles)` recording `state.applied`, and `resetSurfaces()`, and verify tests in `test/models.test.mjs` that copy `agents/*.md` and `codex/agents/*.toml` into a temp fixture, apply a full map, re-read the files and see the new `model:` / `model =` / `model_reasoning_effort =` lines with `sandbox_mode` and `developer_instructions` untouched, that applying the baseline again reproduces the shipped files byte-for-byte, that a CRLF file stays CRLF, that a TOML without the managed header is skipped, that a plugin root outside `<CLAUDE_HOME>/plugins/` is skipped and logged, and a table-driven registry test covering a version-2 array entry, a version-1-style single object entry, an entry with no `installPath`, and an unparsable file

## 3. Detection and background analysis

- [x] 3.1 Implement `probeVersions()` (via `spawnCli` from `scripts/lib/spawn.mjs`, 15 s timeout, first `\d+\.\d+\.\d+` match, `null` on failure, `MY_FLOW_MODELS_FAKE_VERSIONS` seam) and `readMainModels()` (`settings.json` `model`; first top-level `model = "..."` in `config.toml` before any `[section]`), and wire `readModelsCache()` into `check` so the cache is read once per run and never written, and verify unit tests parse `2.1.259 (Claude Code)`, `codex-cli 0.153.4`, a config.toml whose only `model =` sits inside `[agents]` (must yield `null`), a `models_cache.json` fixture (listed slugs and per-slug efforts extracted, hidden slugs dropped, a truncated file yields the empty contribution without throwing, and the fixture's mtime is unchanged after the run), and that `MY_FLOW_MODELS_FAKE_VERSIONS="claude=,codex=0.200.0"` yields `{ claude: null, codex: '0.200.0' }` without spawning (the test sets `PATH` to an empty dir)
- [x] 3.2 Add the optional `--ask-dir <dir>` flag to `scripts/ask.mjs` (parsed next to `--cwd` at `:44`, replacing the default `join(opts.cwd, '.my-flow', 'ask')` at `:49`; nothing else changes), and verify `node scripts/ask.mjs claude --ask-dir <tmp> --timeout 1 probe` exits 1 with a `status: timeout` or `spawn error` line, `ls <tmp>` lists exactly one `*-claude-*.md` artifact, and with `n=$(ls .my-flow/ask | wc -l)` taken before the run, `[ "$(ls .my-flow/ask | wc -l)" = "$n" ]` holds afterwards (`.gitignore:2` ignores `.my-flow/`, so `git status` cannot show this)
- [x] 3.3 Implement `buildPrompt(ctx)` (includes the resolved allowed sets and per-model effort lists), `parseRecommendation(stdout)` and `runAnalysis({ providers, allowed, timeout })` per design D8 (shell out to `MY_FLOW_ASK_SCRIPT` or `<root>/scripts/ask.mjs` with `--prompt-file --cwd --ask-dir --timeout`, child env `MY_FLOW_SKIP_HOOKS` = existing value plus `model-routing`, first valid answer wins, one-line failure reasons), and verify tests with a fixture ask script under the temp root that (a) prints `artifact:`, `status: ok`, a blank line and a fenced JSON answer, yielding a validated map, (b) prints an invalid model name, yielding `invalid: ...` and a fallback to the second provider, (c) exits 1, yielding a failure with all files untouched, and (d) dumps its `MY_FLOW_SKIP_HOOKS` to a file, which then contains `model-routing` (and the pre-existing value when one was set)
- [x] 3.4 Implement `check({ quiet })` per design D7 (lock file `models.lock` with pid and 30 min staleness, full `needApply`: `models.json` validates and the located Claude root or Codex agents dir differs from `state.applied` or the values read back from any located surface differ from `models.json`, first run records `seen` without analysis, unchanged logs `unchanged`, changed picks the provider order, a provider with a non-empty `MY_FLOW_MODELS_FAKE_VERSIONS` entry counts as resolvable without any `where.exe` lookup, success writes `models.json` + applies + sets `pending`, failure increments `analysis.attempts` and records `seen` after three, always sets `lastCheck` and removes the lock, logs `check start (pid N)` first and `check done (pid N)` last), and verify integration tests in `test/models.test.mjs` running `node scripts/models.mjs check --quiet` as a child with `MY_FLOW_HOME`, `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, `MY_FLOW_MODELS_FAKE_VERSIONS` and `MY_FLOW_ASK_SCRIPT` pointing at temp fixtures cover: first run, unchanged, changed + valid answer (models.json exists, both fixture surfaces rewritten, `pending.line` starts with `model routing updated`), changed + failing script (surfaces byte-identical, log contains `analysis failed`, `attempts` is 1), a live lock (second run exits 0 and appends no log line), and drift (a fixture surface hand-edited back to `inherit` while `models.json` and `applied` match: `check` re-applies and logs `re-applied override`)

## 4. CLI entry, hook, install

- [x] 4.1 Create `scripts/models.mjs` (`status [--json]`, `analyze [--provider claude|codex] [--timeout ms] [--dry-run]`, `apply` and `reset` all acquiring `models.lock` exactly like `check` and exiting 1 with `analysis already running (pid N)` when held, `apply`, `reset`, internal `check [--quiet]`, `main()` guard as in `scripts/dashboard.mjs:768`), add `models: 'models.mjs'` and a help line to `scripts/cli.mjs`, add `test/models.test.mjs` to the `test` script in `package.json`, and verify `MY_FLOW_HOME=<empty tmp> node scripts/cli.mjs models status --json` prints JSON with `override: null` and exits 0, `models analyze` with a `models.lock` naming the test's own live pid exits 1 with `analysis already running` and writes nothing, `models reset` with that same lock exits 1 and leaves `models.json` in place, `node scripts/cli.mjs models apply` on a hand-edited `models.json` containing `"model": "gpt-7"` exits 1 and leaves the fixture surfaces unchanged, `models reset` deletes `models.json` and restores the shipped baseline byte-for-byte, and `npm test` runs the new suite
- [x] 4.2 Add `hookTick({ pluginRoot, scriptsDir })` to `scripts/lib/models.mjs` per design D4 (skip on `MY_FLOW_SKIP_HOOKS` containing `model-routing` or `all`, emit and clear `pending`, record `claudePluginRoot`, cheap root-only `needApply` / `needCheck` with `MY_FLOW_MODELS_CHECK_HOURS` default 1, detached spawn of `models.mjs check --quiet`, set `lastSpawn = { at, pid: child.pid }`) and call it from `hooks/session-context.mjs` through `await import('../scripts/lib/models.mjs')` inside the `try/catch` after the change-status lines, appending its lines to `lines`, and verify a `runSessionHook(cwd, env)` helper in `test/models.test.mjs` that always passes `MY_FLOW_HOME`, `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, `MY_FLOW_MODELS_FAKE_VERSIONS` and `MY_FLOW_ASK_SCRIPT` pointing at the temp fixture (the same set as 3.4) plus `CLAUDE_PLUGIN_ROOT`, so a detached check spawned by a hook test never touches the real `~/.my-flow`, `~/.claude` or `~/.codex`, runs `hooks/session-context.mjs` as a child: child wall time < 1000 ms on the second of two consecutive runs, an in-process `hookTick()` call with a cold state returns in < 200 ms, `lastSpawn.at` and a numeric `lastSpawn.pid` written, the routing line appears exactly once across two consecutive runs when `pending` is set, output is still `{}` when nothing is pending and the project has no `changes/`, `MY_FLOW_SKIP_HOOKS=model-routing` with `pending` set prints no routing line, writes no state and leaves `pending` in place, with `scripts/lib/models.mjs` renamed inside a temp copy of the plugin root the hook still prints the change-status JSON and exits 0, and after the test the real `~/.my-flow/models-state.json` does not exist or has an unchanged mtime (existing `test/completion-guard.test.mjs` stays green)
- [x] 4.3 In `scripts/install.mjs` write `<MY_FLOW_HOME>/config.json` per design D9 (`install claude`: `claude.home`; `install codex`: `codex.home`, `codex.agentsDir`; merge with existing content, create `knownModels: []` on first write, `--dry-run` only logs `write <path>`), and verify `MY_FLOW_HOME=<tmp> node scripts/install.mjs codex --dry-run` prints a `write .../config.json` line and creates nothing, and `MY_FLOW_HOME=<tmp> CODEX_HOME=<tmp2> node scripts/install.mjs codex` leaves `<tmp>/config.json` with `codex.agentsDir` equal to `<tmp2>/agents` and `knownModels: []`

## 5. Documentation and spec

- [x] 5.1 Update `README.md`: CLI table row for `models status|analyze|apply|reset`, the SessionStart hook row (throttled detached check, one summary line, `MY_FLOW_SKIP_HOOKS=model-routing`, `MY_FLOW_MODELS_CHECK_HOURS`), a short "Model routing" subsection under "Subagent roles" describing `~/.my-flow/models.json`, `models.log`, `knownModels` and `MY_FLOW_HOME`, the `--ask-dir` flag in "Cross-model advisor", the `scripts/` line in "Repository layout", and a troubleshooting row "subagents run an unexpected model" pointing to `models status` and `models reset`, and verify `grep -c "models status" README.md` is at least 3 and `grep -n "MY_FLOW_MODELS_CHECK_HOURS\|model-routing\|--ask-dir\|knownModels" README.md` shows each term at least once
- [x] 5.2 Mirror the role table rows, the `models` CLI row, the SessionStart hook row and the troubleshooting row into `README.de.md`, `README.es.md`, `README.fr.md`, `README.ja.md`, `README.ko.md`, `README.zh-CN.md` and `README.zh-TW.md` in each file's language, and verify `for f in README.*.md; do grep -q "models status" "$f" && grep -q "inherit" "$f" && grep -q "model-routing" "$f" || echo "missing in $f"; done` prints nothing
- [x] 5.3 Review the delta spec `changes/agent-model-routing/specs/model-routing/spec.md` (written in mf-plan) against the implemented behaviour, correct any WHEN / THEN scenario that no longer matches the code or the test plan rows, and verify `node scripts/spec.mjs validate agent-model-routing` reports no errors and every scenario names a check that exists in `test/models.test.mjs` or in task 6.2

## 6. End-to-end simulated upgrade and final gate

- [x] 6.1 Add the end-to-end test to `test/models.test.mjs` using `runSessionHook` from 4.2 (fixture env set plus `CLAUDE_PLUGIN_ROOT` and `MY_FLOW_MODELS_CHECK_HOURS=0` for both variants): success fixture = temp `MY_FLOW_HOME` whose `models-state.json` records older versions and no `models.json`, fake newer versions, fixture Claude cache (`plugins/installed_plugins.json` + `plugins/cache/my-flow/my-flow/9.9.9/agents/*.md`) and Codex `agents/*.toml`, `MY_FLOW_ASK_SCRIPT` = the valid-answer script; run the hook, poll `models.log` for a `check start` / `check done (pid N)` pair with `N` equal to `lastCheck.pid` up to 15 s, then before every subsequent hook run and before `reset` poll until the count of `check done` lines equals the number of hook runs so far: run the hook twice more, run `node scripts/models.mjs reset`, run the hook once more; failure fixture = a fresh `MY_FLOW_HOME` with the same older-version state and no `models.json`, fresh copies of the surface files, `MY_FLOW_ASK_SCRIPT` = the exit-1 script; run the hook, poll `models.log` for a `check start` / `check done (pid N)` pair with `N` equal to `lastCheck.pid` up to 15 s, run the hook again, and poll until the count of `check done` lines equals 2 before asserting, and verify the suite asserts for the success fixture: first hook run under 1000 ms, `models.json` validates, both fixture surfaces carry the recommended values, `models.log` has a `check start (pid N)` / `check done (pid N)` pair with the same `N`, written after `lastSpawn.at`, plus `updated`, with `N` equal to `lastCheck.pid` and to `lastSpawn.pid` (primary launcher) and `lastCheck.at` newer than `lastSpawn.at`, the second hook run prints exactly one line starting with `model routing updated`, the third prints none, and the hook run after `reset` prints one `model routing reset to baseline` line with `status --json` reporting `override: null`; and for the failure fixture: both surfaces byte-identical to the shipped baseline, `models.log` contains `analysis failed`, no `models.json`, and the second hook run prints exactly one `model routing analysis failed` line
- [x] 6.2 Run the real-CLI checks on this machine: (0) after asking the user once (because `install codex` rewrites `~/.codex/config.toml`, `hooks.json` and `AGENTS.md`, outside the proposal's allowed write set), run the user-side install steps from design Rebuild / Re-run: `node scripts/install.mjs codex`, `node scripts/install.mjs claude`, then `claude plugin update my-flow@my-flow` (or `claude plugin marketplace add <repo> && claude plugin install my-flow@my-flow`); (a) `node scripts/cli.mjs models analyze --dry-run` (default provider order); (b) from a temp directory outside the repo run `env -u CLAUDECODE -u CLAUDE_CODE_ENTRYPOINT MY_FLOW_MODELS_CHECK_HOURS=0 claude -p "reply ok" --max-turns 1`, then `MY_FLOW_MODELS_CHECK_HOURS=0 codex exec --skip-git-repo-check -C <tmp> "reply ok"`, and if the log shows no `check start` at all after `codex exec`, ask the user to run interactive `codex` once from that directory with `MY_FLOW_MODELS_CHECK_HOURS=0` set in their shell and `/exit` (SessionStart may not fire in exec mode; the executor cannot drive the TUI); (c) `node scripts/cli.mjs models analyze` (real call, no `--dry-run`; the one real model call of this task; writes `~/.my-flow/models.json` and applies it; `models reset` is available afterwards if the user prefers the baseline), confirm `node scripts/cli.mjs models status --json` shows a non-null override, then `node scripts/cli.mjs models apply` (expected to log `re-applied` or `unchanged`), then one more `env -u CLAUDECODE -u CLAUDE_CODE_ENTRYPOINT MY_FLOW_MODELS_CHECK_HOURS=0 claude -p "reply ok" --max-turns 1`; (b) Codex leg and (c) are re-run as task 7.4 with the scheduled-task launcher after 7.1-7.3 (tick this box together with 7.4; the launcher that shipped is the `schtasks` one of design D4), and verify (0) `grep -l "scripts/lib/models.mjs" "$(node -e "console.log(require(require('os').homedir()+'/.claude/plugins/installed_plugins.json').plugins['my-flow@my-flow'][0].installPath)")/hooks/session-context.mjs"` prints the path and `grep -L '^model = ' ~/.codex/agents/*.toml` lists all four files; (a) exits 0, prints a recommendation that passes validation, writes a prompt and result artifact under `~/.my-flow/ask/`, and `node scripts/cli.mjs models status --json` is identical before and after (mtimes of `~/.codex/agents/*.toml` and of the cache `agents/*.md` unchanged); (b) after each CLI session `~/.my-flow/models.log` has a `check start (pid N)` / `check done (pid N)` pair with the same `N`, written after `lastSpawn.at`, with `N` equal to `lastCheck.pid` in `models-state.json` and `lastCheck.at` newer than `lastSpawn.at` (under the `primary` launcher also `N` equal to `lastSpawn.pid`; under `schtasks` `lastSpawn.pid` is `null` and `lastSpawn.launcher` is `schtasks`); (c) the `model:` line of each cache `agents/<role>.md` equals `roles.<role>.claude.model` in `~/.my-flow/models.json`, the `model_reasoning_effort =` line of each `~/.codex/agents/<role>.toml` equals `roles.<role>.codex.model_reasoning_effort`, the `check done` written after that last session is preceded by `unchanged` (no re-apply, no second analysis), and design D4 names the launcher that shipped
  - resolved in mf-plan iteration 3 (was blocked): (0) and (a) passed; (b) Claude leg passed with the primary launcher (pair pid 20060, first run only, ~300 ms). (b) Codex leg: after the user trusted the hooks in /hooks, both `codex exec` and the TUI run the SessionStart hook, but the detached check was killed about one second after `check start` under both the primary spawn (pid 31980) and `cmd.exe /c start /b` (pid 28220): Codex tears down the hook's job object. Fixed on the way: `install codex` no longer writes the `state` key that Codex 0.154 rejects in hooks.json; new or changed hooks must be trusted once in /hooks. User decision: a Task Scheduler launcher registered at install time (design D4, D9; group 7). (c) not started; re-run as 7.4.
- [x] 6.3 Targeted verification of the whole change: `node scripts/build.mjs --check`, `npm test`, `node scripts/spec.mjs validate agent-model-routing`, and a diff review for placeholder TODOs, `test.skip` / `.only` and stub returns, then cleanup of own diff only and re-run the same three commands, and verify all three exit 0 with the grep for `TODO\|test\.skip\|\.only(` over changed files printing nothing
- [x] 6.4 Independent verification through `/my-flow:mf-verify agent-model-routing` (separate context), and verify a report under `.my-flow/verify/` contains `Verdict: PASS`

## 7. Launcher amendment: Task Scheduler (mf-plan iteration 3; runs before the 6.3 / 6.4 final gate)

- [x] 7.1 Rewrite `launchCheck(script, { platform = process.platform, schtasksBin = process.env.MY_FLOW_SCHTASKS ?? 'schtasks' })` in `scripts/lib/models.mjs` per design D4: on win32 with `config.launcher.task` set and `MY_FLOW_MODELS_LAUNCHER` not `primary`, run `schtasks /run /tn <task>` synchronously (`spawnSync`, timeout 750 ms, `windowsHide`; when `MY_FLOW_SCHTASKS` names a script run it as `node <script> /run /tn <task>`), return `{ launcher: 'schtasks', pid: null }` on exit 0, otherwise record `state.launcherFailedAt = now`, log `schtasks /run failed (<code or timeout>), falling back to primary` once for that failure and fall through; while `launcherFailedAt` is younger than `MY_FLOW_MODELS_CHECK_HOURS` (default 1 h) skip the `schtasks` attempt entirely (no call, no log) and go straight to primary, and clear `launcherFailedAt` on the next successful `/run`; everywhere else the plain detached `spawn(process.execPath, [script, 'check', '--quiet'])` with `{ launcher: 'primary', pid }`; delete the `cmd-start` branch; `hookTick` records `lastSpawn = { at, pid, launcher }`; `runSessionHook` and the fixture env in `test/models.test.mjs` set `MY_FLOW_MODELS_LAUNCHER=primary`, and the e2e assertion keeps `N === lastSpawn.pid` only for `primary`, and verify new unit tests in `test/models.test.mjs` with a fixture script that writes its argv to a file and exits 0 or 1: task set + exit 0 gives `schtasks` with `pid: null` and the recorded argv `/run /tn my-flow-models-check`; task set + exit 1 gives `primary` with a numeric pid, one `falling back to primary` line in `models.log` and `launcherFailedAt` set; task set + a fixture that sleeps 2 s gives `primary`, a `hookTick()` wall time still < 1000 ms and `launcherFailedAt` set, and a second `hookTick()` within the hour never calls the fixture (no new argv file) and adds no log line; no `config.launcher` gives `primary` and no argv file; `MY_FLOW_MODELS_LAUNCHER=primary` with a task set gives `primary` and no argv file; `platform: 'linux'` with a task set gives `primary` and no argv file; `grep -c "cmd-start" scripts/lib/models.mjs test/models.test.mjs` prints 0 for both, and `npm test` is green
  - critic follow-up (iteration 3, applied after execution): ownership as implemented is `hookTick` -> `launchCheck(script, { state, now })` mutating only `state.launcherFailedAt`, single state write in `hookTick` (design D4 updated); the cooldown case deletes `lastSpawn` between the two `hookTick()` calls with `MY_FLOW_MODELS_CHECK_HOURS` unset, so `needCheck` is true and only `launcherFailedAt` prevents the call (test `launchCheck: schtasks when a task is registered ...`)
- [x] 7.2 Add `--home <dir>` to `parseArgs` in `scripts/models.mjs`, applied as `process.env.MY_FLOW_HOME = opts.home` before any library call; in `scripts/install.mjs` add `registerLauncher()` per design D9, called by both `install claude` and `install codex` on win32 only: refuse (print why, record nothing) when `%SystemRoot%\System32\conhost.exe` is missing, otherwise run `schtasks /create /f /tn my-flow-models-check /tr "conhost.exe --headless \"<process.execPath>\" \"<ROOT>/scripts/models.mjs\" check --quiet --home \"<MY_FLOW_HOME>\"" /sc once /st 00:00` (fall back to `/sc onlogon` only if `/sc once` is refused, and say so in the log), and on success `writeConfig({ launcher: { task, root: ROOT, node: process.execPath, home, registered } })`; on failure print the schtasks output and record nothing; `--dry-run` prints the `schtasks /create` line only; `uninstall claude` and `uninstall codex` remove their own `config.<tool>.home`, and when neither home remains run `schtasks /delete /tn my-flow-models-check /f` and clear `config.launcher` (no shim to delete), otherwise print `scheduled task kept for the other surface`; `models status` prints `launcher: schtasks task <name> (root <root> exists:<bool>, node exists:<bool>, registered <iso>, query <ok|missing>)` or `launcher: primary (no scheduled task)` and adds a `launcher` object to `--json`, and verify `MY_FLOW_HOME=<tmp> node scripts/install.mjs claude --dry-run` prints exactly one line containing `schtasks /create /f /tn my-flow-models-check` whose `/tr` starts with `conhost.exe --headless` and ends with `--home "<tmp>"`, and creates no file, `schtasks /query /tn my-flow-models-check` gives the same result before and after the dry run, a new test in `test/models.test.mjs` checks the dry-run output of both installers, that `node scripts/models.mjs check --quiet --home <tmp2>` (fixture env, fake versions) writes `models-state.json` and `models.log` under `<tmp2>` and nothing under the fixture's default home, and that `models status --json` on a fixture `config.json` with a `launcher` block reports `launcher.task` and `exists` flags, and `npm test` is green
  - critic follow-up (applied after execution): uninstall branches now covered by the test `uninstall --dry-run: keeps the task while another surface remains, deletes it otherwise, writes nothing` (both `claude.home` + `codex.home` -> `scheduled task kept for the other surface`, no `schtasks /delete`; only `claude.home` -> exactly one `schtasks /delete /tn my-flow-models-check /f` line; config.json byte-identical in both cases)
- [x] 7.3 Update `README.md` (hook row: on Windows the check is started through the scheduled task `my-flow-models-check` registered by `install claude` / `install codex`, falling back to a detached child; `install claude` / `install codex` / `uninstall` rows mention the task; troubleshooting row `check never completes under Codex, or "check start" never appears`: run `schtasks /query /tn my-flow-models-check`, check `models status` for the launcher root and node, re-run `node scripts/install.mjs claude` or `codex`) and mirror the hook row, the install / uninstall rows and the troubleshooting row into `README.de.md`, `README.es.md`, `README.fr.md`, `README.ja.md`, `README.ko.md`, `README.zh-CN.md` and `README.zh-TW.md` in each file's language, and verify `for f in README.md README.*.md; do grep -q "my-flow-models-check" "$f" && grep -q "schtasks /query" "$f" || echo "missing in $f"; done` prints nothing and `grep -c "my-flow-models-check" README.md` is at least 3
- [x] 7.4 Re-run task 6.2 on this machine with the scheduled-task launcher: (0) after asking the user once, `node scripts/install.mjs codex` and `node scripts/install.mjs claude` from this checkout (registers the task), then `claude plugin update my-flow@my-flow`; (b) from a temp directory outside the repo `env -u CLAUDECODE -u CLAUDE_CODE_ENTRYPOINT MY_FLOW_MODELS_CHECK_HOURS=0 claude -p "reply ok" --max-turns 1`, then `MY_FLOW_MODELS_CHECK_HOURS=0 codex exec --skip-git-repo-check -C <tmp> "reply ok"` (if no `check start` appears, ask the user to run interactive `codex` once from that directory with `MY_FLOW_MODELS_CHECK_HOURS=0` in their shell and `/exit`); (c) `node scripts/cli.mjs models analyze` (the one real model call), `models status --json` non-null override, `models apply`, then one more `env -u CLAUDECODE -u CLAUDE_CODE_ENTRYPOINT MY_FLOW_MODELS_CHECK_HOURS=0 claude -p "reply ok" --max-turns 1`; `models reset` is available afterwards if the user prefers the baseline; a `check start` without `check done` under `schtasks` is reported as a blocker, not worked around, and verify `schtasks /query /tn my-flow-models-check` exits 0 and `models status --json` shows `launcher.task` with both `exists` flags true; after each of the three sessions `models-state.json` has `lastSpawn.launcher === 'schtasks'` with `lastSpawn.pid === null`, and `~/.my-flow/models.log` has a `check start (pid N)` / `check done (pid N)` pair with the same `N`, written after `lastSpawn.at`, with `N` equal to `lastCheck.pid` and `lastCheck.at` newer than `lastSpawn.at`; no console window appeared during the analysis; `schtasks /query /tn my-flow-models-check /v /fo list` shows a `Task To Run` line starting with `conhost.exe --headless` that contains the current `process.execPath` and this checkout's `scripts/models.mjs`; the `model:` line of each cache `agents/<role>.md` equals `roles.<role>.claude.model` in `~/.my-flow/models.json`, the `model_reasoning_effort =` line of each `~/.codex/agents/<role>.toml` equals `roles.<role>.codex.model_reasoning_effort`, the `check done` written after the last session is preceded by `unchanged`, and 6.2 is ticked in the same step
  - evidence (2026-09-10): (0) task registered by both installers, `schtasks /query` exit 0, status launcher rootExists/nodeExists true, cache hook carries models.mjs, four TOMLs without `model =`; (b) claude -p: pair pid 33704, launcher schtasks, pid null, lastCheck newer; that check detected codex 0.153.4 -> 0.154.0 and ran the analysis by codex (valid, models.json written and applied); codex exec: pair pid 6016, launcher schtasks, `unchanged`; (c) `models analyze` by claude, status override non-null, apply `unchanged`, extra claude -p: pair pid 32964 with `unchanged` before `check done`, all four roles match models.json on both surfaces; `Task To Run` = conhost.exe --headless with the current node path and this checkout; window: the user was not at the screen, objective probe instead: a node started the same way (`conhost.exe --headless node -e ...`) reports MainWindowHandle 0 (no window), consistent with the architect measurement recorded in D9
  - critic follow-up (verdict written before execution, received after): survival past the hook is proven by a task-started check that ran the full analysis: `check start (pid 33704)` 11:21:31.061Z -> `analysis by codex: valid recommendation` 11:22:08.077Z -> `model routing updated ...` -> `check done (pid 33704)` 11:22:08.108Z (37 s, same pid, `N === lastCheck.pid`, `lastSpawn.launcher === schtasks`, `models.json` with `analyzedBy: codex`); hook trust after the (0) reinstall is proven by the Codex-triggered pair `pid 6016` (Codex skips untrusted hooks silently, so a running check means the trust held); `seen.cliVersions` recorded by the task equals the shell probe (`{"claude":"2.1.259","codex":"0.154.0"}`, match true), so the PATH risk does not apply on this machine; the window check used the objective MainWindowHandle 0 probe because the user was away
