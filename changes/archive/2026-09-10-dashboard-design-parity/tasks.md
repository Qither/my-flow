## 1. Baseline and guards

- [x] 1.1 Snapshot `web/` and `package.json` to the session scratchpad and run `npm test` once before any edit, and verify the snapshot directory contains `index.html`, `app.css`, `app.mjs`, `md.mjs` and the run ends with `fail 0` (record the pass count for later comparison)
- [x] 1.2 Create `test/styles.lib.mjs` exporting `emittedClasses(text)` (template-depth-aware `class="..."` scanner where an interpolated string literal counts only when the non-space character before its quote is `?` or `:`, plus `classList.add|toggle('...')` scan, supplement `['add','del','ctx','meta']` for `${l.kind}` at `web/app.mjs:413`, ignore prefix `language-`), `cssClasses(css)` (strips `/* */` comments and quoted strings first), `darkTwinMismatch(css)` and `orphanClasses(css, emittedSet)`, and `test/styles.test.mjs` importing them with unit cases for the literal patterns at `web/app.mjs:207,284,322,428,432` and `web/md.mjs:97,119` (the `app.mjs:284` fixture asserts `abandoned` is not collected; a fixture containing the `web/app.css:2` comment asserts `css` is not collected), no coverage assertion yet, and verify `node --test test/styles.test.mjs` passes the unit cases and `node -e` importing `emittedClasses` and `cssClasses` from `test/styles.lib.mjs` and passing `readFileSync` of the real `web/` files prints exactly `open` and `flat` as the emitted classes with no rule
- [x] 1.3 In one edit add `.md li.task.open .box` and `.diff-tree.flat li.file` rules to `web/app.css` and, to `test/styles.test.mjs`, the forward-coverage assertion, the assertion that `darkTwinMismatch(app.css)` is `null` using the exact normalisation of design D3 (prelude exactly `@media (prefers-color-scheme: dark)`, prefix stripping, sorted declarations minus `color-scheme`, multiset equality, anchored on `app.css:29-39` vs `41-52`, `116` vs `117`, `257` vs `259`), the no-network scan over `readdirSync('web')`, and the `package.json` no-`dependencies` assertion, add the file to the `test` script in `package.json`, and verify `npm test` is green, and `node -e` importing `darkTwinMismatch` from `test/styles.lib.mjs` and passing `readFileSync` of a scratchpad copy of `app.css` with one declaration changed inside the media block only prints that selector rather than `null`

## 2. Tokens, reset and control grammar

- [x] 2.1 Add the tokens of design D1 to `:root` and, where they differ, to both dark blocks (`--pressed-bg/fg`, `--outline-w/off`, `--disabled`, `--sb-page/inner/code`, `--check`, `--switch-w/h`, `--menu-pad`, `--add-bg`, `--del-bg`), replace the literals at `web/app.css:116-117,243,245,253,263,275` with token reads and delete the nav-active twin, and verify `node --test test/styles.test.mjs` passes the dark-twin assertion and `grep -c "pressed-bg" web/app.css` is at least 3 (root, media block, attribute block)
- [x] 2.2 Insert the reset of design D2 after the token blocks (box-sizing on pseudo-elements, `appearance: none` and inherited font on form controls, `border-radius: 0`, `:focus:not(:focus-visible)`, summary marker removal, fieldset and legend, abbr, the `label:has(input)` cursor with the accepted Firefox 121+ floor noted in a comment), and verify `npm test` is green and, in a browser on `#/changes`, a `<details>` pasted into the console shows no marker and a clicked button shows no focus ring while a tabbed-to button shows the outline
- [x] 2.3 Rewrite the control grammar block: `button, .btn` with variants `.primary`, `.neutral`, `.reverse`, `.icon`, state selectors written once with `:is(:hover, .sg-hover)`, `:is(:active, .sg-active)`, `:is(:focus-visible, .sg-focus)`, disabled via `--disabled`, `.nav-list a` and `.nav-list a.active` on `--pressed-*`, and verify `npm test` is green, `grep -c "sg-hover" web/app.css` is at least 1, and `grep -c 'button:hover' web/app.css` is 0 (every button hover goes through `:is`)
- [x] 2.4 Restyle panels, cards, notices, warnings, progress bar, tags (`.tag`, `.lime`, `.warn`, `.st`, `.latest`, `.md a.via`), `.conn-dot`, `.crumbs`, `.editor textarea`, `pre`, `code`, `hr`, `table` (design D6 block scrolling with `padding: 1px`) per the class map in design D4, and verify `npm test` is green (markdown golden tests untouched) and in a browser `#/changes` shows 2 px tag borders, `#/file/...` shows the textarea without shadow, a narrow pipe table shows all four outer edges at full 2 px, and a wide pipe table in a spec scrolls inside itself instead of widening the page
- [x] 2.5 Draw the task box (design D5), restyle `.diff-tree .dir-toggle` as a flat control that opts out of the pressed translate (`transform: none` on `:is(:active, .sg-active)`), `.diff-tree li.file.selected` on `--pressed-*` with the `.st` / `.latest` inversions rewritten explicitly as `.diff-tree li.file.selected .tag.st, .diff-tree li.file.selected .tag.latest { background: var(--pressed-fg); color: var(--pressed-bg); border-color: var(--pressed-fg); }` (hover keeps the existing lime inversions), and the patch rows on `--add-bg` / `--del-bg`, and verify `npm test` is green and in a browser a tasks.md preview shows drawn open and checked boxes and `#/diff` shows the toggle without a border, the toggle does not move when clicked, and the selected row is in the pressed colours in both themes

## 3. Scrollbars

- [x] 3.1 Implement the scrollbar matrix of design D9: body horizontal track with top border, `:horizontal` variants inside both dark blocks, `::-webkit-scrollbar-corner` for `.sidebar`, `.editor textarea`, `.diff-left`, `.patch`, `pre`, `table` and `.sg-scroll`, `.listbox-menu` and `table` bars, and the extended `@supports not selector(::-webkit-scrollbar)` fallback list, and verify `node --test test/styles.test.mjs` passes the dark-twin assertion and `grep -c "::-webkit-scrollbar-corner" web/app.css` is at least 6, then in a browser a wide table, a wide code block and a wide patch each show a horizontal bar in the theme colours in light and in dark

## 4. Markdown elements from magick.css

- [x] 4.1 Add the element rules of design D11 (blockquote with footer and cite, dl, figure and figcaption, kbd, mark, abbr, details and summary, sup and sub, `pre .line-number`, fieldset, legend, label) using only tokens, and verify `npm test` is green, `test/markdown.test.mjs` still asserts a blockquote renders as escaped text, and `grep -c "https\?://" web/app.css` is 0

## 5. Listbox widget

- [x] 5.1 Create `web/ui.mjs` exporting `TYPEAHEAD_MS`, `listboxInit` and the pure `listboxNext` of design D7 (returning `changed` and `handled`) with nothing executed at import time, create `test/ui.test.mjs` with the key, type-ahead, pointer, no-change and `handled: false` (unknown key, Tab while closed) cases listed in design D10 plus the `import('../web/ui.mjs')` without `document` check, add it to the `test` script, extend the no-network scan in `test/styles.test.mjs` to also assert `ui.mjs` is among the names `readdirSync('web')` returns, and verify `node --test test/ui.test.mjs` passes and `node -e "import('./web/ui.mjs').then(m => console.log(Object.keys(m)))"` prints the three exports without error
- [x] 5.2 Add `mountListbox(root, { onChange })` to `web/ui.mjs` with the event rules of design D7 (trigger `keydown` calls `preventDefault()` when the reducer result's `handled` is true and `key` is not `Tab`; trigger `click` is the only pointer toggle; menu `pointerdown` calls `preventDefault()` so focus stays on the trigger; document `pointerdown` closes only when `!root.contains(ev.target)`; `focusout` closes only when `relatedTarget` is outside `root`; `aria-activedescendant` is removed while closed; attribute writes for `hidden`, `aria-expanded`, `aria-selected`, `data-active`, `.listbox-value`, `data-value`; `set`, `get`, `destroy`) and the `.listbox`, `.listbox-trigger`, `.listbox-value`, `.listbox-caret`, `.listbox-menu`, `.listbox-menu[hidden] { display: none }` (no other `display` on the menu), `.listbox-option`, `.select-trigger` rules, and verify `node --test test/ui.test.mjs test/styles.test.mjs` is green (the styles test does not yet require an emitter for the listbox classes) and the file still imports in Node without a DOM
- [x] 5.3 Replace the theme markup at `web/index.html:25-34` with the listbox of design D7 (label span with `id="theme-label"`), remove the `.theme select` and `.select-wrap` rules, wire the theme in `web/app.mjs` in the order mount first (`state.themeBox = mountListbox($('theme-listbox'), { onChange: onThemeChange })`, the handler named `onThemeChange` because `boot()` already declares `const onChange` at `web/app.mjs:529`) then `applyTheme(normalizeTheme(storage.get(THEME_KEY)))`, with `applyTheme` calling `state.themeBox?.set(choice)`, extend the keyboard guard at `web/app.mjs:481` to skip a `role="combobox"` target, add `/ui.mjs` content-type and status assertions to `test/ui.test.mjs`, and verify `npm test` is green and in a browser: choosing Dark then reloading paints dark at first frame and the trigger label reads "Dark" immediately after the reload, choosing Auto clears `localStorage['my-flow.theme']`, Enter and Space on the focused trigger each toggle the menu exactly once (no reopen after commit), clicking an option with the pointer commits it and closes the menu, the closed trigger carries no `aria-activedescendant`, ArrowDown / Home / End / type-ahead `l` / Escape / Enter behave as the delta spec scenarios describe, `j` and `k` on the focused trigger on `#/diff` change no selection, and after `Object.defineProperty(window, 'localStorage', { get() { throw new Error('denied'); } })` in the console the control still switches the theme

## 6. Form-control kit

- [x] 6.1 Add the checkbox, radio, `.switch` (track and thumb), text input, textarea and placeholder rules of design D12 with hover, checked, focus-visible and disabled states through the same `:is()` markers, and verify `npm test` is green and a `<label class="switch"><input type="checkbox" role="switch"><span class="switch-track"><span class="switch-thumb"></span></span></label>` pasted into a page's console renders a 48 x 24 square-cornered track whose thumb moves on click

## 7. Styleguide route

- [x] 7.1 Add `styleguide` to `parseRoute` and `shouldRefetch` (returning false), the `pageStyleguide()` static page and `wireStyleguide()` mount per design D8 with every group and state listed there (using `.sg-hover`, `.sg-active`, `.sg-focus` markers and `.sg-static` for the open menu; `wireStyleguide` stores the mount's `destroy` as `state.unmountStyleguide` and `render()` calls `state.unmountStyleguide?.()` next to `state.unbindDiffKeys?.()` at `web/app.mjs:517`), the `.sg*` layout rules, and the `.sg-link` in `web/index.html`, add the `parseRoute` and `shouldRefetch` cases to `test/ui.test.mjs`, and verify `npm test` is green and `http://127.0.0.1:4321/#/styleguide` renders every group with captions, the live listbox works, the network panel shows no `/api/` request while the page is open, editing a file under `changes/` does not re-render it, and after navigating from `#/styleguide` to `#/changes` a `pointerdown` anywhere on the page no longer reaches the demo listbox (browser checks use the running dashboard on port 4321 after a reload, per Rebuild / Re-run)
- [x] 7.2 Enable the reverse-coverage assertion in `test/styles.test.mjs` (`orphanClasses(app.css, emitted)` over the union of `index.html`, `app.mjs`, `md.mjs` and `ui.mjs` is empty, allowlist empty) and fill any gap by adding the missing example to the styleguide rather than to the allowlist, and verify `npm test` is green and `node -e` importing `orphanClasses` and `emittedClasses` from `test/styles.lib.mjs` and passing `readFileSync` of a scratchpad copy of `app.css` with `.zzz-orphan{}` appended prints `['zzz-orphan']`

## 8. Acceptance

- [x] 8.1 Use the dashboard already running on port 4321 against this repository (`.my-flow/state/dashboard.json`; `start` would report the port busy) or `node scripts/cli.mjs dashboard stop` then `start`, reload the browser, capture `#/styleguide` in light and in dark (theme control, not the OS), or, when the executor has no browser tool, ask the user to open that URL and capture both themes, then hand or point the user to both captures and the qiujm-web header control and select menu references, and verify the user has accepted the parity in writing; tick this box only after that acceptance

## 9. Final gate

- [x] 9.1 Targeted verification of the whole change (`npm test`, `node scripts/spec.mjs validate dashboard-design-parity`, `grep -rn "https\?://" web/` returns nothing, `package.json` has no `dependencies`, and the Rebuild / Re-run steps of design.md) and verify every command's output is recorded in the session
- [x] 9.2 Cleanup of own diff only (compare `web/`, `test/ui.test.mjs`, `test/styles.test.mjs`, `test/styles.lib.mjs` and `package.json` with the 1.1 snapshot; remove leftover markers, console logs and unused selectors), then re-verify with `npm test` and verify the run is green with at least the 1.1 pass count plus the new files' cases
- [x] 9.3 Independent verification report says PASS (`.my-flow/verify/`) and verify the report cites fresh `npm test` output and the styleguide captures from 8.1
