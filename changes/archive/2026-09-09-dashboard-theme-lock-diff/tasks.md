## 1. Baselines and snapshots

- [x] 1.1 Record the test baseline with `npm test 2>&1 | tail -8` and verify the output reads `pass 70` and `fail 0`
- [x] 1.2 Copy `web/`, `test/dashboard.test.mjs`, `test/helpers.mjs`, `scripts/dashboard.mjs`, `src/skills/dashboard.md`, `package.json`, all eight `README*.md` and the current `.my-flow/state/current-change.json` into `<scratchpad>/baseline-tld/` (these files are untracked, so `git diff` cannot show this change) and verify `diff -r <scratchpad>/baseline-tld/web web` prints nothing and the copied state file names the change and stage to restore in task 8.2

## 2. Server: git detection and the diff routes

- [x] 2.1 Add the git plumbing to `scripts/dashboard.mjs` (`GIT_TIMEOUT_MS = 5000`, `GIT_MAX_BUFFER = 8 * 1024 * 1024`, `MAX_PATCH_BYTES = 512 * 1024`, `EMPTY_TREE`, a `git(root, args)` wrapper over `spawnSync` passing `{ cwd: root, encoding: 'utf8', windowsHide: true, timeout: GIT_TIMEOUT_MS, maxBuffer: GIT_MAX_BUFFER }`, since design D1 requires both limits and D6's `ENOBUFS` branch depends on `maxBuffer`) and detect git once in `startServer` via `git rev-parse --is-inside-work-tree` plus `git rev-parse --verify HEAD`, exposing `git` in the `/api/health` payload and the `hello` SSE event, and verify the wrapper's option object literally contains `timeout` and `maxBuffer`, and that a foreground server on this repository answers `"git":true` while one on a fresh `mkdtemp` directory answers `"git":false`
- [x] 2.2 Implement `diffList(root, base)` from `git diff <base> --name-status -z -M --`, `git diff <base> --numstat -z -M --` and `git ls-files --others --exclude-standard -z`, producing the `files` array of design D10 with `status`, `oldPath`, `added`, `deleted`, `binary` and `untracked`, and drop every path starting `.my-flow/` from both the tracked and the untracked list unconditionally, never relying on a `.gitignore` rule (design D4), and verify a `node -e` call against this repository lists `scripts/dashboard.mjs` as `?` and `README.md` as `M` with non-zero counts, and that the same call against a temporary repository holding a committed `.my-flow/ask/x.md` and no `.gitignore` returns no path beginning `.my-flow/`
- [x] 2.3 Implement `diffPatch(root, base, entry)` using `git diff <base> -M -- <oldPath?> <path>` for tracked files and the synthesised `new file` patch of design D5 for untracked ones, gating the untracked branch on `lstatSync(abs).isFile()` before any read so a symlink is never followed and non-regular entries return `patch: null` with `skipped: 'not-a-regular-file'`, and honouring `MAX_PATCH_BYTES`, the binary flags and an `ENOBUFS` result, and verify a `node -e` call returns a patch starting `diff --git` for a tracked modified file, one containing `--- /dev/null` for an untracked file, and `skipped: 'not-a-regular-file'` with `patch: null` for an untracked symlink pointing outside the root
- [x] 2.4 Wire `GET /api/diff` and `GET /api/diff/file` into the `handle` ladder before the `/api/` fallthrough, answering `404 no-git` without git, `400 not-in-diff` for a path the list does not contain and `500 git-failed` when a git call exits non-zero, and verify a foreground server on this repository answers 200 for `/api/diff`, 400 for `/api/diff/file?path=package-lock.json`, and 200 with a patch for a path taken from the list
- [x] 2.5 Create `test/diff.test.mjs` with a `gitFixture()` builder (`gitInit` from `test/helpers.mjs`, a committed file, then one modified, one deleted, one untracked text file, one untracked file holding a NUL byte, an untracked symlink where the platform allows one, and `.my-flow/` content with no `.gitignore`) guarded by `hasGit()` with `t.skip` when git is missing, covering design test-plan cases T6 to T10 including T9b and T9c, with the no-git case T10 reading `git` from `/api/health` first and calling `t.skip` naming the fixture root when it is `true` rather than asserting a false pass, add that file to the `test` script in `package.json` and nothing else, and verify `npm test 2>&1 | tail -8` reports `fail 0` above 70 total, `git diff -- package.json` shows exactly one changed line, and the T10 case reports either a pass or an explicit skip, never a silent success

## 3. Server: the execute lock

- [x] 3.1 In one edit, change the fixture stage in `test/dashboard.test.mjs:90` from `execute` to `mf-plan` and add `lockOf(root)` (reading `readState` per call, returning null unless the stage is `execute`) plus the 423 refusal in `handleFilePost`, placed between the `writable(root, r.rel)` check and the existence check with `{ ok:false, error:'locked', change, stage, message }`; the fixture edit ships in the same task because landing the refusal while the fixture still says `execute` would turn the three existing save tests red in between, and verify the lock line sits after the `not-writable` return and before the `no-such-file` return, and that `npm test 2>&1 | tail -8` reports `fail 0` with a total above the 70 baseline plus the group 2 additions (task 2.5 already raised the count)
- [x] 3.2 Make `handleFileGet` report `writable: writable(root, rel) && !lock` and `lockReason` (the message only when the file would otherwise be writable, null otherwise) and verify a foreground server on a fixture at stage `execute` returns `writable:false` with a non-null `lockReason` for a change file and `lockReason:null` for an archived file
- [x] 3.3 Add lock tests T1 to T5 to `test/dashboard.test.mjs` (423 with the file byte-identical, `writable:false` plus `lockReason`, the lock lifting after the state file is rewritten with no server restart, 403 still beating 423 for a non-writable target, and 423 before the mtime check with no `content` in the body), each setting stage `execute` itself rather than relying on the fixture default, and verify `npm test 2>&1 | tail -8` reports `fail 0` with a total above the 70 baseline plus the group 2 additions

## 4. Theme

- [x] 4.1 Rewrite the dark rules in `web/app.css` per design D8: guard the `@media (prefers-color-scheme: dark)` token block with `:root:not([data-theme="light"])`, add a `:root[data-theme="dark"]` twin with the same six tokens plus `color-scheme: dark`, add `:root[data-theme="light"] { color-scheme: light }`, and give `.nav-list a.active` and `body::-webkit-scrollbar-thumb` the same guard and twin, and verify `diff <scratchpad>/baseline-tld/web/app.css web/app.css` shows no change to the `:root` light token values and `grep -c 'data-theme' web/app.css` returns at least 6
- [x] 4.2 Add the inline pre-paint script to `<head>` of `web/index.html` before the stylesheet link, plus the three-button theme group and a hidden `Diff` nav entry in the sidebar, and verify `grep -c 'data-theme-choice' web/index.html` returns 3 and `node --test test/dashboard.test.mjs` still passes the offline no-external-URL test
- [x] 4.3 Export `THEME_KEY`, `normalizeTheme` and `themeAttr` from `web/app.mjs` and wire the control inside `boot()` so a click applies the theme to the document first (sets or removes `data-theme`, updates `aria-pressed`) and persists it second, with `localStorage.setItem` and `removeItem` each wrapped in their own `try`/`catch` so the control still switches the theme when storage is denied, as the "Storage being unavailable is not an error" scenario requires, and give the `diffView` toggle the same guard, and verify `node -e "import('./web/app.mjs').then(m=>console.log(m.normalizeTheme('x'), m.themeAttr('system')))"` prints `system null` and that every `localStorage` call in `web/app.mjs` and `web/index.html` sits inside a `try` block

## 5. Client: lock handling and the diff page

- [x] 5.1 Show `lockReason` as a notice in `pageFile` when the field is set and widen the `file` branch of `shouldRefetch` so every file route reacts to `.my-flow/state/current-change.json`, and verify `node -e` shows `shouldRefetch({page:'file',path:'specs/dashboard/spec.md'}, ['.my-flow/state/current-change.json'])` returning true
- [x] 5.2 Add the pure exported helpers `parseUnifiedDiff` and `buildTree` plus the `diff` branch of `parseRoute` and `shouldRefetch` to `web/app.mjs`, and verify `node -e` parses a two-hunk patch into two hunks with correct old and new line numbers and prints `{ page: 'diff', path: null }` for `#/diff` and the decoded path for `#/diff/web%2Fapp.css`
- [x] 5.3 Implement `pageDiff(path)` in `boot()` (fetch `/api/diff`; left pane as tree or flat list per `localStorage['my-flow.diffView']` with a persisted toggle, collapsible directories and a Refresh button; fetch `/api/diff/file` for the selected path and render it with `escapeHtml` and the `add` / `del` / `ctx` / `hunk` classes; a one-line explanation when git is absent) and unhide the sidebar `Diff` entry when `hello` reports `git:true`, and verify a foreground server on this repository serves `#/diff` and `curl` of the page shell plus `/api/diff` returns this repository's changed files
- [x] 5.4 Add the diff, tree, patch and theme-control styles to `web/app.css` (`.diff` two-pane grid mirroring `.editor`, `.diff-tree`, `.patch` grid rows readable in both themes) and add the pure-helper tests T11 to T14 to `test/diff.test.mjs`, and verify `grep -rE "https?://" web/` is empty and `npm test 2>&1 | tail -8` reports `fail 0`

## 6. Skill text and generated outputs

- [x] 6.1 Add the theme toggle, the execute lock and the diff page to the prose of `src/skills/dashboard.md` without touching either fenced command block, then run `npm run build` followed by `npm run check`, and verify `check` reports the outputs up to date, `skills/dashboard/SKILL.md` carries the new prose, and both `MY-FLOW:CLAUDE` and `MY-FLOW:CODEX` blocks are byte-identical to the baseline copy

## 7. Documentation in eight languages

- [x] 7.1 Extend the `## Dashboard` section of `README.md` with three short sentences or bullets covering the theme toggle, the execute lock and the diff page (including that the diff page refreshes automatically only for the watched directories and otherwise has a Refresh button), leaving every code block unchanged, and verify `diff <scratchpad>/baseline-tld/README.md README.md` (the task 1.2 snapshot; `git diff` would also show the earlier uncommitted change's hunks) touches no line inside a fenced block
- [x] 7.2 Make the equivalent addition in the dashboard section of `README.de.md`, `README.es.md` (`## Panel web`), `README.fr.md` (`## Tableau de bord`), `README.ja.md`, `README.ko.md`, `README.zh-CN.md` and `README.zh-TW.md`, each in that file's own language, and verify `for f in README*.md; do echo $f $(grep -c '^## ' $f); done` prints the same heading counts as the baseline copies and each file mentions all three features

## 8. End-to-end evidence

- [x] 8.1 Write `<scratchpad>/theme-lock-diff-e2e.mjs`, a headless-Chrome DevTools-protocol driver modelled on the existing `e2e-browser.mjs` and `scrollbar-shots.mjs`, and capture the theme evidence with it (`Emulation.setEmulatedMedia` forcing dark then choosing `light`, forcing light then choosing `dark`, then a reload, and a final pass with `Storage.prototype.setItem` redefined to throw), and verify each screenshot shows the intended palette, the transcript records the computed `background-color` of `body` at each step, and the storage-denied pass still switches the theme in both directions
- [x] 8.2 Capture the lock evidence with the dashboard open on a change file: run `node scripts/spec.mjs stage dashboard-theme-lock-diff execute` and verify the page loses Save and states the reason without a reload, then run `node scripts/spec.mjs stage dashboard-theme-lock-diff mf-plan` and verify Save returns without a reload, and restore the stage recorded in task 1.2
- [x] 8.3 Capture the diff evidence against this repository (the `#/diff` page in tree form, in flat-list form after the toggle, and a selected file whose patch shows coloured added and removed lines with numbers) and a second run against a temporary non-git directory, and verify the transcript shows `git:false`, no `Diff` entry and the one-line explanation for that second run
- [x] 8.4 Write the transcript and screenshot paths to `.my-flow/verify/dashboard-theme-lock-diff-e2e-<timestamp>.md` and verify the file lists every capture from tasks 8.1 to 8.3 with its screenshot filename

## 9. Final gate

- [x] 9.1 Targeted verification of the whole change (`npm test`, `npm run check`, `grep -rE "https?://" web/` empty, `package.json` still declaring no `dependencies`, `node scripts/spec.mjs validate dashboard-theme-lock-diff`) and verify every command succeeds and the test total is the 70 pre-existing plus the new cases with `fail 0`
- [x] 9.2 Read every changed file for placeholder TODOs, `test.skip` or `.only`, stub returns and unimplemented branches, clean up only this change's own diff, then re-run task 9.1's commands and verify they all still pass
- [x] 9.3 Run `/my-flow:mf-verify dashboard-theme-lock-diff` in a fresh context and verify the report written under `.my-flow/verify/` states PASS
- [x] 9.4 Run `node scripts/spec.mjs stage dashboard-theme-lock-diff done` and verify `node scripts/spec.mjs status` shows the change at stage `done` with every task ticked
