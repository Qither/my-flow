## 1. Stop hook

- [x] 1.1 In `hooks/completion-guard.mjs` add `const GOAL_HANDOFF_RE = /^\s*\/goal\s+\S/m;` directly after `COMPLETION_CLAIM_RE` (`:42-43`), change the allow line (`:76`) to `if (!message || GOAL_HANDOFF_RE.test(message) || !COMPLETION_CLAIM_RE.test(message)) allow();`, and add one sentence to the header comment naming `GOAL_HANDOFF_RE`: a message that hands off the session goal (a line starting with `/goal `) matches `GOAL_HANDOFF_RE` and is never treated as a completion claim, and verify `grep -c "GOAL_HANDOFF_RE" hooks/completion-guard.mjs` prints 3 and, in a scratch project with two unticked tasks and a fresh `stage execute` state, piping `{"cwd":"<root>","last_assistant_message":"Paste this to keep the session on task, then say \"continue\":\n/goal Complete every unchecked task in changes/demo/tasks.md. Done only when every box is ticked."}` into `node hooks/completion-guard.mjs` prints `{}` while the same pipe with `"All tasks are done and verified."` still prints a `[my-flow execute-guard]` block
- [x] 1.2 In `test/completion-guard.test.mjs` add, after the "does not block a message without a completion claim" test (`:46-49`), two execute-guard tests: `execute-guard allows the /goal handoff message although it contains completion words` (message = the handoff shape from task 1.1, expect `{}`) and `execute-guard still blocks a claim that only mentions /goal mid-sentence` (message = `All tasks are done; the /goal was active throughout.`, expect `decision` `block` and reason starting `[my-flow execute-guard]`), and verify `node --test --test-reporter=tap test/completion-guard.test.mjs | grep '^# pass\|^# fail'` prints `# pass 17` (baseline 15) and `# fail 0`, and `npm test` is green

## 2. execute source skill

- [x] 2.1 In `src/skills/execute.md` delete Load step 4 (`:21-22`) so `## 1. Load` ends with the `git status` step, and insert as the new first step of `## 3. Task loop` (before "For each pending task") the sentence "Run `{{CALL:spec}} stage <name> execute` once before task 1; this writes `.my-flow/state/current-change.json` with a fresh `updated` timestamp and arms the Stop-hook execute-guard. Never edit that file by hand.", keeping the per-task refresh in loop step 3 and rewording the Backstop paragraph's "written in step 1.4" reference (`:47-49` is removed in 2.2; check the Backstop paragraph at `:50-58` no longer refers to step 1.4; its `(step 4.5)` reference to the final gate stays), and verify `awk '/^## 1. Load/{f=1;next} /^## /{f=0} f' src/skills/execute.md | grep -c "stage"` prints 0, `awk '/^## 3. Task loop/{f=1;next} /^## /{f=0} f' src/skills/execute.md | grep -c "stage <name> execute"` prints 2, and `grep -c "1\.4" src/skills/execute.md` prints 0
- [x] 2.2 In the `<!-- MY-FLOW:CLAUDE -->` block of section 2 of `src/skills/execute.md` remove the three-line clause starting "Entering from `{{CALL:mf-plan}} --fast --go`" (`:47-49`) and append to the preceding sentence the words "This handoff is the default for every entry into `execute`, including a run that arrived from `{{CALL:mf-plan}} --fast --go`; it pauses once for the paste.", and verify `grep -c "declining the goal" src/skills/execute.md` prints 0, `grep -c -- "--fast --go" src/skills/execute.md` prints 1, that line lies inside the CLAUDE block (`awk '/MY-FLOW:CLAUDE/,/\/MY-FLOW:CLAUDE/' src/skills/execute.md | grep -c -- "--fast --go"` prints 1), and the CODEX block is byte-identical to `git show HEAD:src/skills/execute.md` for its range (`diff <(awk '/MY-FLOW:CODEX/,/\/MY-FLOW:CODEX/' src/skills/execute.md) <(git show HEAD:src/skills/execute.md | awk '/MY-FLOW:CODEX/,/\/MY-FLOW:CODEX/')` prints nothing)

## 3. mf-plan source skill

- [x] 3.1 Rewrite `## Continue into execute (--go)` in `src/skills/mf-plan.md` (`:93-111`) per design D4: shared prose (valid only with `--fast` after the handoff; continue into `{{CALL:execute}} <name>`, which composes the goal, hands it off as it always does, arms the execute-guard before task 1, and ends with its Run report; `--team` / `--worktree` unavailable, stop and point at `{{CALL:execute}} <name>`), a CLAUDE block (invoke `my-flow:execute <name>` with the Skill tool and follow the loaded text including its section-2 goal stop, never paraphrase, record line `Continuing into execute (--go): goal handoff applies.`), and a CODEX block (follow `{{CALL:execute}} <name>` by reference from Load through the final gate with `get_goal` / `create_goal` running normally), and verify `awk '/^## Continue into execute/{f=1;next} /^## /{f=0} f' src/skills/mf-plan.md | grep -c "stage\|standing instruction\|goal stop is skipped\|declining"` prints 0, the same awk piped to `grep -c "goal handoff applies"` prints 1, `grep -c "MY-FLOW:" src/skills/mf-plan.md` still prints 8, and `awk '/MY-FLOW:CLAUDE/,/\/MY-FLOW:CLAUDE/' src/skills/mf-plan.md | grep -c "Skill tool"` still prints 1

## 4. Core block and READMEs

- [x] 4.1 In `src/core/core.md:74-76` change "then waits for the user to paste it; it never tries to set it itself." to "then waits for the user to paste it, for every run including one entered from `mf-plan --fast --go`; it never tries to set it itself.", and verify `grep -c "including one entered from" src/core/core.md` prints 1 and the bullet still starts with "- One loop authority per session"
- [x] 4.2 In `README.md` replace the sentence "Exception: a run entered from `mf-plan --fast --go` skips the `/goal` stop and relies on the Stop-hook backstop alone." (`:118`) with "A run entered from `mf-plan --fast --go` also pauses once for the `/goal` paste." and replace "which skips the `/goal` stop" in the `execute` row (`:153`) with "which also pauses for the `/goal` paste", and verify `grep -c "skips the \`/goal\` stop\|relies on the Stop-hook backstop alone" README.md` prints 0 and `grep -c "pauses once for the \`/goal\` paste\|also pauses for the \`/goal\` paste" README.md` prints 2
- [x] 4.3 Mirror the two README edits of task 4.2 into `README.de.md`, `README.es.md`, `README.fr.md`, `README.ja.md`, `README.ko.md`, `README.zh-CN.md` and `README.zh-TW.md`, each in its own language at `:118` and `:153`, and verify `for f in README.de.md README.es.md README.fr.md README.ja.md README.ko.md README.zh-CN.md README.zh-TW.md; do n=$(sed -n '118p;153p' "$f" | grep -c -- "--fast --go"); s=$(grep -c "überspringt den \`/goal\`-Stopp\|\`/goal\`-Stopp überspringt\|omite la parada de \`/goal\`\|saute l'arrêt \`/goal\`\|\`/goal\` の停止をスキップ\|\`/goal\` 정지를 건너\|跳过 \`/goal\` 停顿\|跳過 \`/goal\` 停點" "$f"); [ "$n" = 2 ] && [ "$s" = 0 ] || echo "bad in $f (rows $n, skip-phrases $s)"; done` prints nothing

## 5. Build, tests, installed surfaces

- [x] 5.1 Run `node scripts/build.mjs`, then `node scripts/build.mjs --check` and `npm test`, and verify both commands exit 0, `npm test` is green, `grep -c "declining the goal\|1\.4" skills/execute/SKILL.md codex/skills/my-flow-execute/SKILL.md` prints 0 for both, `grep -c "goal handoff applies" skills/mf-plan/SKILL.md` prints 1 and the same grep over `codex/skills/my-flow-mf-plan/SKILL.md` prints 0 (Claude-only block), `grep -c "spec stage\|standing instruction" skills/mf-plan/SKILL.md` prints 0 inside the `--go` section (`awk '/^## Continue into execute/{f=1;next} /^## /{f=0} f' skills/mf-plan/SKILL.md | grep -c "stage\|standing"` prints 0), and `grep -c "including one entered from" claude/CLAUDE.block.md` prints 1
- [x] 5.2 Install the rendered surfaces: `node scripts/install.mjs claude` (upserts the working-agreement block into `~/.claude/CLAUDE.md`), `node scripts/install.mjs codex --dry-run` then `node scripts/install.mjs codex`, and refresh the Claude plugin cache with `claude plugin update my-flow@my-flow` (record the exact output; if the marketplace source is not this checkout, record that and how the cache was refreshed), and verify `grep -c "including one entered from" "$HOME/.claude/CLAUDE.md"` prints 1, and both `grep -c "GOAL_HANDOFF_RE" "<installPath>/hooks/completion-guard.mjs"` and `grep -c "goal handoff applies" "<installPath>/skills/mf-plan/SKILL.md"` are non-zero where `<installPath>` is printed by `node -e "const r=require(require('os').homedir()+'/.claude/plugins/installed_plugins.json').plugins['my-flow@my-flow'];console.log((Array.isArray(r)?r[0]:r).installPath)"`
  - evidence: marketplace source is this checkout (directory); `claude plugin update my-flow@my-flow` answered "already at the latest version (0.1.0)" and left the cache at commit 44b1cb2 (version-only comparison), so the cache was refreshed with `claude plugin uninstall my-flow@my-flow` followed by `claude plugin install my-flow@my-flow`; afterwards the cache reports sha 09ff314, `GOAL_HANDOFF_RE` count 3 in the cached hook and `goal handoff applies` count 1 in the cached mf-plan skill; `~/.claude/CLAUDE.md` phrase count 1; codex surface installed

## 6. Specs

- [x] 6.1 Confirm the delta specs written during planning validate: `node scripts/spec.mjs validate execute-goal-default` exits 0 with no errors, `changes/execute-goal-default/specs/execute-goal/spec.md` has three ADDED requirements each with at least one WHEN / THEN scenario, and `changes/execute-goal-default/specs/mf-plan-fast/spec.md` has one REMOVED requirement (with a `**Reason**:` line) named exactly "`--go` continues into execute without the goal stop" plus one ADDED requirement "`--go` continues into execute through the goal handoff", and verify `grep -c "^### Requirement:" changes/execute-goal-default/specs/execute-goal/spec.md` prints 3 and `grep -c "^### Requirement:" changes/execute-goal-default/specs/mf-plan-fast/spec.md` prints 2
- [x] 6.2 In `specs/mf-plan-fast/spec.md` change the `## Purpose` paragraph's ending "and the `--go` hand-over that continues straight into the execute flow without the interactive `/goal` stop." to "and the `--go` hand-over that continues straight into the execute flow through its normal `/goal` handoff.", touching nothing below `## Requirements`, and verify `grep -c "without the interactive" specs/mf-plan-fast/spec.md` prints 0, `grep -c "normal \`/goal\` handoff" specs/mf-plan-fast/spec.md` prints 1, and, having copied the file to `.my-flow/tmp/mf-plan-fast.before.md` before editing, `diff -U0 .my-flow/tmp/mf-plan-fast.before.md specs/mf-plan-fast/spec.md | grep -c '^[-+][^-+]'` prints 2 (`specs/` is untracked in git, so `git diff` shows nothing for it)

## 7. Final gate

- [x] 7.1 Targeted verification of the whole change: `node scripts/build.mjs --check`, `npm test`, `node scripts/spec.mjs validate execute-goal-default`, and a scan of the lines this change added (`git diff -U0 -- hooks/completion-guard.mjs test/completion-guard.test.mjs src/skills/execute.md src/skills/mf-plan.md src/core/core.md README.md README.de.md README.es.md README.fr.md README.ja.md README.ko.md README.zh-CN.md README.zh-TW.md specs/mf-plan-fast/spec.md | grep "^+" | grep -n "TODO\|test\.skip\|\.only("`) for placeholder markers, then cleanup of own diff only and a re-run of the same three commands, and verify all three exit 0 both times and the scan prints nothing
  - evidence: both passes: `build.mjs --check` exit 0 ("generated files are up to date"), `npm test` 136 pass / 0 fail, `spec.mjs validate execute-goal-default` ok; the added-lines scan printed nothing; diff review found nothing to clean (18 files, +90/-67, all traceable to tasks 1.1-6.2)
- [x] 7.2 Independent review through `/my-flow:mf-verify execute-goal-default` in a separate context, and verify a report under `.my-flow/verify/` contains `Verdict: PASS`, then run `node scripts/spec.mjs stage execute-goal-default done` and verify `node -e "console.log(require('./.my-flow/state/current-change.json').stage)"` prints `done`
