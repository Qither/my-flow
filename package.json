{
  "name": "my-flow",
  "version": "0.1.0",
  "description": "Lightweight cross-tool workflow layer for Claude Code and Codex CLI, built on native features with a specs/ + changes/ intent layer (structure borrowed from OpenSpec, no tool dependency).",
  "type": "module",
  "private": true,
  "bin": {
    "my-flow": "scripts/cli.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check",
    "test": "node --test test/spec.test.mjs test/completion-guard.test.mjs test/dashboard.test.mjs test/markdown.test.mjs test/diff.test.mjs test/models.test.mjs test/styles.test.mjs test/ui.test.mjs test/plugin.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
