## Context

<!-- Current state and constraints that shape the approach. See proposal.md for motivation. -->

## Goals / Non-Goals

**Goals:**

**Non-Goals:**

## Decisions

<!-- Key design decisions with rationale and alternatives considered (PLAN-DR: principles,
     decision drivers, viable options). Tasks point at these ids with "- design: D-01"; the ids
     never change when a decision is reworded or reordered. -->

### D-01 — <!-- short decision title -->

### D-02 — <!-- short decision title -->

## Risks / Trade-offs

## Do-Not-Touch

<!-- Modules, directories, or files this change must not modify. Required; write
     "none, because ..." if truly empty. -->

## Rebuild / Re-run After Change

<!-- What must be regenerated, rebuilt, cooked, cleared, or re-run after the edits, and when
     (e.g. after config changes: regenerate project files and rebuild <target>; after shader
     or asset changes: rebuild derived data and re-run <tests>). Required; "none, because ..."
     if truly empty. -->

## File Ownership

<!-- Only when a team executes this change: two or more disjoint groups of files, each with
     one owner and the task ids it covers. Delete this section otherwise. -->
